---
phase: "16"
plan: "02"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - contracts/deployments.json
autonomous: false
requirements:
  - DEPL-03
  - DEPL-04
must_haves:
  truths:
    - "All 4 contracts deployed to Polygon Amoy testnet"
    - "ASKToken owner is the deployer address"
    - "StakingManager.token() returns ASKToken address"
    - "SkillRegistry.stakingManager() returns StakingManager address"
    - "Attribution.stakingManager() returns StakingManager address"
    - "Contracts are visible on block explorer"
  artifacts:
    - path: "contracts/deployments.json"
      provides: "Deployed contract addresses and metadata"
      contains:
        - "ASKToken address (0x...)"
        - "StakingManager address (0x...)"
        - "SkillRegistry address (0x...)"
        - "Attribution address (0x...)"
        - "chainId: 80002"
  key_links:
    - from: "deploy-all.cjs output"
      to: "contracts/deployments.json"
      via: "fs.writeFileSync"
    - from: "deployed contracts"
      to: "Polygonscan"
      via: "manual verification"
user_setup:
  - task: "Ensure .env file exists with PRIVATE_KEY"
    instructions: |
      Before running deployment, ensure contracts/.env exists with:
      - PRIVATE_KEY: Your deployer wallet private key
      - POLYGON_AMOY_RPC: Optional (uses default if not set)
      - POLYGONSCAN_API_KEY: Optional (verification skipped if not set)
---

<objective>
Deploy contracts to Polygon Amoy and verify on Polygonscan.
</objective>

<context>
@contracts/scripts/deploy-all.cjs
@contracts/deployments.json
@.planning/phases/16-polygon-amoy-deploy/16-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 01: Deploy contracts to Polygon Amoy</name>
  <files>contracts/deployments.json</files>
  <read_first>
    - contracts/.env.example (verify required variables)
    - contracts/scripts/deploy-all.cjs (review script logic)
  </read_first>
  <action>
    Execute the deployment script to Polygon Amoy testnet.
    
    **Pre-flight checklist before running:**
    1. Verify contracts/.env exists with PRIVATE_KEY configured
    2. Verify PRIVATE_KEY has sufficient MATIC for gas on Amoy testnet
    3. Verify network connectivity to https://rpc-amoy.polygon.technology
    
    **Run deployment:**
    ```bash
    cd F:/skillsSecurity && npx hardhat run contracts/scripts/deploy-all.cjs --network polygonAmoy
    ```
    
    **Expected output:**
    - ASKToken deployed to: 0x...
    - StakingManager deployed to: 0x...
    - SkillRegistry deployed to: 0x...
    - Attribution deployed to: 0x...
    - Attribution wired to StakingManager
    - Deployment info saved to contracts/deployments.json
    
    **If POLYGONSCAN_API_KEY is set:**
    - Verification attempts for each contract
    - Success/failure status for each verification
    
    **If deployment fails:**
    - Check PRIVATE_KEY is valid (starts with 0x)
    - Check wallet has sufficient MATIC on Amoy
    - Check RPC URL is accessible
  </action>
  <verify>
    <automated>cat contracts/deployments.json 2>/dev/null | grep -q "ASKToken" && cat contracts/deployments.json | grep -q "StakingManager" && cat contracts/deployments.json | grep -q "SkillRegistry" && cat contracts/deployments.json | grep -q "Attribution" && echo "PASS"</automated>
  </verify>
  <done>
    All 4 contracts deployed successfully:
    - ASKToken address present in deployments.json
    - StakingManager address present in deployments.json
    - SkillRegistry address present in deployments.json
    - Attribution address present in deployments.json
    - Deployer address recorded
    - Network and chainId recorded
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02: Verify contracts on Polygonscan</name>
  <files>contracts/deployments.json</files>
  <read_first>
    - contracts/deployments.json (get deployed addresses)
  </read_first>
  <action>
    Verify that all deployed contracts are visible and verified on Polygonscan.
    
    **Step 1: Get deployed addresses from deployments.json**
    
    **Step 2: Check each contract on Polygonscan**
    Visit the following URLs (replace with actual addresses from deployments.json):
    - ASKToken: https://www.oklink.com/amoy/address/[ASKTOKEN_ADDRESS]#code
    - StakingManager: https://www.oklink.com/amoy/address/[STAKINGMANAGER_ADDRESS]#code
    - SkillRegistry: https://www.oklink.com/amoy/address/[SKILLREGISTRY_ADDRESS]#code
    - Attribution: https://www.oklink.com/amoy/address/[ATTRIBUTION_ADDRESS]#code
    
    **Step 3: Verify source code matches**
    For each contract, verify:
    - Source code tab shows contract code
    - Compile version matches 0.8.20
    - Constructor arguments match what was passed during deployment
    
    **Step 4: Verify contract functionality (optional but recommended)**
    Use "Read Contract" to verify basic state:
    - ASKToken: owner() returns deployer address
    - StakingManager: token() returns ASKToken address
    - SkillRegistry: stakingManager() returns StakingManager address
    - Attribution: stakingManager() returns StakingManager address
    
    **If verification failed:**
    - Run manual verification: `npx hardhat verify <ADDRESS> <ARGS> --network polygonAmoy`
    - Check POLYGONSCAN_API_KEY is valid
    - Check contract address is correct
  </action>
  <verify>
    <automated>grep -q "contracts": contracts/deployments.json && node -e "const d=JSON.parse(require('fs').readFileSync('contracts/deployments.json')); ['ASKToken','StakingManager','SkillRegistry','Attribution'].forEach(c=>{if(!d.contracts[c]){console.error('MISSING:',c);process.exit(1);}if(!d.contracts[c].match(/^0x[a-fA-F0-9]{40}$/)){console.error('INVALID:',c,d.contracts[c]);process.exit(1);}}); console.log('PASS');"</automated>
  </verify>
  <done>
    All contracts verified on block explorer:
    - Each contract has source code visible
    - Contract addresses are correct format (0x... 40 hex chars)
    - deployments.json records all addresses with correct chainId (80002)
  </done>
</task>

</tasks>

<verification>
1. deployments.json exists with all 4 contract addresses
2. All addresses are valid Ethereum addresses (0x... 40 hex chars)
3. Chain ID in deployments.json is 80002 (Polygon Amoy)
</verification>

<success_criteria>
- All 4 contracts deployed to Polygon Amoy (chainId 80002)
- deployments.json contains all contract addresses
- Contracts accessible and functional on Amoy testnet
- Contracts can be viewed on Polygonscan
</success_criteria>

<output>
After completion, create `.planning/phases/16-polygon-amoy-deploy/16-02-SUMMARY.md`
</output>