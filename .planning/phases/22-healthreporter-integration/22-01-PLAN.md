---
phase: 22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/RevenueSplit.sol
  - contracts/ReputationBadges.sol
  - contracts/SelfSustainingEcosystem.sol
  - contracts/interfaces/IRevenueSplit.sol
  - contracts/interfaces/IReputationBadges.sol
autonomous: true
requirements:
  - OPS-07
  - OPS-08

must_haves:
  truths:
    - "RevenueSplit can receive ETH and split to creator/referrer/auditor/dispute fund/platform"
    - "ReputationBadges can issue non-transferable ERC-721 badges to users"
    - "SelfSustainingEcosystem manages role-based incentives and self-operation metrics"
  artifacts:
    - path: "contracts/RevenueSplit.sol"
      provides: "Service fee distribution with 70/10/5/5/10 split"
      min_lines: 120
    - path: "contracts/ReputationBadges.sol"
      provides: "Non-transferable reputation badges (ERC-721)"
      min_lines: 100
    - path: "contracts/SelfSustainingEcosystem.sol"
      provides: "Role-based ecosystem with incentives and health reports"
      min_lines: 150
  key_links:
    - from: "RevenueSplit.deposit()"
      to: "msg.sender addresses"
      via: "ETH transfer split"
      pattern: "payable.*split.*7000|1000|500|500|1000"
    - from: "ReputationBadges.issueBadge()"
      to: "blockchain state"
      via: "ERC721 mint"
      pattern: "_safeMint.*revert.*Non-transferable"
    - from: "SelfSustainingEcosystem"
      to: "ReputationBadges"
      via: "badge issuance on role activation"
      pattern: "issueBadge.*CREATOR|AUDITOR|REFERRER"
---

<objective>
创建无代币架构的三个核心合约：RevenueSplit（服务费分账）、ReputationBadges（链上声誉徽章）、SelfSustainingEcosystem（自维持生态）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/skillsSecurity/contracts/HealthReporter.sol
@F:/skillsSecurity/contracts/ASKToken.sol
@F:/skillsSecurity/.planning/phases/22-healthreporter-integration/22-CONTEXT.md

# Key design from CONTEXT.md

## RevenueSplit fee split:
- CREATOR_FEE = 7000 (70%)
- REFERRER_FEE = 1000 (10%)
- AUDITOR_FEE = 500 (5%)
- DISPUTE_FEE = 500 (5%)
- PLATFORM_FEE = 1000 (10%)

## ReputationBadges badge types:
- SKILLSHARP_100 (100+ successful deliveries)
- VERIFIED_DEVELOPER (certified developer)
- TOP_RATED (4.5+ rating)
- EARLY_ADOPTER (early user)
- SECURITY_AUDITOR (security auditor)
- CODE_REVIEWER (code reviewer)

## SelfSustainingEcosystem roles:
- CREATOR, AUDITOR, REFERRER, DISPUTER, NODE, CURATOR
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RevenueSplit.sol - Service fee distribution contract</name>
  <files>
    - contracts/RevenueSplit.sol
    - contracts/interfaces/IRevenueSplit.sol
    - test/RevenueSplit.t.sol
  </files>
  <read_first>
    @F:/skillsSecurity/.planning/phases/22-healthreporter-integration/22-CONTEXT.md
    @F:/skillsSecurity/contracts/ASKToken.sol (for import patterns)
  </read_first>
  <behavior>
    - Test 1: deposit() receives ETH and splits to correct recipients based on basis points
    - Test 2: withdraw() allows each role to claim their accumulated share
    - Test 3: Re-entrancy protection prevents double-withdrawal
    - Test 4: Emergency withdrawal by owner drains remaining funds
  </behavior>
  <action>
    Create `contracts/RevenueSplit.sol` with:

1. Constants (basis points):
   - CREATOR_FEE = 7000 (70%)
   - REFERRER_FEE = 1000 (10%)
   - AUDITOR_FEE = 500 (5%)
   - DISPUTE_FEE = 500 (5%)
   - PLATFORM_FEE = 1000 (10%)

2. State:
   - mapping(address => uint256) public creatorShares
   - mapping(address => uint256) public referrerShares
   - mapping(address => uint256) public auditorShares
   - mapping(address => uint256) public disputeFund
   - uint256 public platformBalance

3. Events:
   - FeeDeposited(creator, referrer, auditor, amount)
   - Withdrawn(recipient, role, amount)
   - EmergencyWithdraw(amount)

4. Functions:
   - deposit(address creator, address auditor, address referrer) external payable
     * Split incoming ETH by basis points
     * Accumulate in respective share mappings
   - withdraw() external
     * Transfer accumulated share to caller based on role
   - emergencyWithdraw(address to) external onlyOwner
     * Drain remaining balance to address

5. Use ReentrancyGuard from OpenZeppelin
6. Use pull payment pattern to avoid reentrancy attacks

Create corresponding test file with 4 behavior tests.
  </action>
  <verify>
    <automated>cd F:/skillsSecurity && npx hardhat test test/RevenueSplit.t.sol --grep "RevenueSplit"</automated>
  </verify>
  <done>
    RevenueSplit.deposit splits ETH correctly (70/10/5/5/10)
    Each role can withdraw their accumulated share
    Re-entrancy protection works
    Emergency withdrawal drains contract
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ReputationBadges.sol - Non-transferable reputation badges</name>
  <files>
    - contracts/ReputationBadges.sol
    - contracts/interfaces/IReputationBadges.sol
    - test/ReputationBadges.t.sol
  </files>
  <read_first>
    @F:/skillsSecurity/contracts/ASKToken.sol (for import patterns)
    OpenZeppelin ERC721 documentation
  </read_first>
  <behavior>
    - Test 1: issueBadge() mints badge to recipient with correct badge type
    - Test 2: transferFrom() always reverts with "Non-transferable" message
    - Test 3: Only issuer can call issueBadge()
    - Test 4: Badge evidence is stored and queryable
  </behavior>
  <action>
    Create `contracts/ReputationBadges.sol` with:

1. Inherit from ERC721 and Ownable

2. BadgeType enum:
   - SKILLSHARP_100
   - VERIFIED_DEVELOPER
   - TOP_RATED
   - EARLY_ADOPTER
   - SECURITY_AUDITOR
   - CODE_REVIEWER

3. State:
   - address public issuer (default = owner)
   - mapping(uint256 => BadgeType) public badgeTypes
   - mapping(uint256 => string) public badgeEvidence
   - mapping(address => mapping(BadgeType => uint256)) public userBadges

4. BadgeInfo struct:
   - badgeType: BadgeType
   - evidence: string
   - issuedAt: uint256

5. Functions:
   - issueBadge(address recipient, BadgeType badgeType, string memory evidence) external onlyIssuer
     * Mint NFT to recipient
     * Store badge type and evidence
     * Update user badge count
   - getBadgeInfo(uint256 tokenId) external view returns (BadgeInfo memory)
   - getUserBadgeCount(address user, BadgeType badgeType) external view returns (uint256)
   - setIssuer(address newIssuer) external onlyOwner

6. CRITICAL: Override transferFrom to always revert:
   ```solidity
   function transferFrom(address, address, uint256) public pure override {
       revert("Badges are non-transferable");
   }
   ```
   Same for safeTransferFrom.

7. Events:
   - BadgeIssued(recipient, badgeType, tokenId, evidence)
   - IssuerChanged(newIssuer)

Create corresponding test file with 4 behavior tests.
  </action>
  <verify>
    <automated>cd F:/skillsSecurity && npx hardhat test test/ReputationBadges.t.sol --grep "ReputationBadges"</automated>
  </verify>
  <done>
    issueBadge() mints NFT with correct type and evidence
    transferFrom reverts with "Non-transferable" for all calls
    Only owner/issuer can issue badges
    Badge info is queryable by token ID
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create SelfSustainingEcosystem.sol - Role-based incentives</name>
  <files>
    - contracts/SelfSustainingEcosystem.sol
    - test/SelfSustainingEcosystem.t.sol
  </files>
  <read_first>
    @F:/skillsSecurity/contracts/HealthReporter.sol (for health report patterns)
    @F:/skillsSecurity/.planning/phases/22-healthreporter-integration/22-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: registerRole() activates a role for user with correct tier
    - Test 2: recordContribution() increases contribution score
    - Test 3: calculateRewards() returns correct reward based on tier and contributions
    - Test 4: generateHealthReport() creates report with system metrics
  </behavior>
  <action>
    Create `contracts/SelfSustainingEcosystem.sol` with:

1. Role enum:
   - CREATOR (skill creator)
   - AUDITOR (skill verifier)
   - REFERRER (promotion referrer)
   - DISPUTER (dispute resolver)
   - NODE (node operator)
   - CURATOR (content curator)

2. RoleTier enum:
   - BRONZE (base tier)
   - SILVER (mid tier)
   - GOLD (top tier)

3. State:
   - struct RoleInfo { role, tier, contributions, lastActive, registeredAt }
   - mapping(address => RoleInfo) public userRoles
   - mapping(address => uint256) public roleRewards
   - uint256 public totalContributions

4. Configuration:
   - struct TierConfig { minContributions, rewardMultiplier, badgeThreshold }
   - TierConfig[3] public tierConfigs (Bronze/Silver/Gold)
   - mapping(Role => uint256) public roleBaseReward

5. Functions:
   - registerRole(Role role) external
     * Create RoleInfo for user with BRONZE tier
   - upgradeTier(Role role) external
     * Check contributions and upgrade if thresholds met
   - recordContribution(Role role, uint256 amount) external
     * Increment contribution score
     * Update lastActive timestamp
   - calculateRewards(address user, Role role) external view returns (uint256)
     * Based on tier multiplier and contribution score
   - claimRewards() external
     * Transfer accumulated rewards
   - generateHealthReport() external returns (HealthReport memory)
     * Returns system-wide metrics for self-operation monitoring

6. Events:
   - RoleRegistered(user, role)
   - RoleUpgraded(user, role, oldTier, newTier)
   - ContributionRecorded(user, role, amount, total)
   - RewardClaimed(user, amount)

7. Reference ReputationBadges for automatic badge issuance:
   * On role tier upgrade, automatically issue badge via ReputationBadges

Create corresponding test file with 4 behavior tests.
  </action>
  <verify>
    <automated>cd F:/skillsSecurity && npx hardhat test test/SelfSustainingEcosystem.t.sol --grep "SelfSustaining"</automated>
  </verify>
  <done>
    User can register roles (CREATOR/AUDITOR/REFERRER/DISPUTER/NODE/CURATOR)
    Tier upgrade triggers when contribution thresholds met
    Rewards calculated correctly based on tier multiplier
    Health report generates system-wide metrics
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → RevenueSplit | Untrusted ETH sent, must be split correctly |
| issuer → ReputationBadges | Trusted: only issuer can mint badges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Reentrancy | RevenueSplit.withdraw | mitigate | Use ReentrancyGuard, pull payment pattern |
| T-22-02 | Spoofing | ReputationBadges.issueBadge | mitigate | Only owner/issuer can call, require address != 0 |
| T-22-03 | Repudiation | SelfSustainingEcosystem | accept | Health reports are for monitoring, not critical |
</threat_model>

<verification>
1. `npx hardhat compile` succeeds without errors
2. All test files pass with `npx hardhat test`
3. Contract sizes within limits
</verification>

<success_criteria>
- RevenueSplit.deposit correctly splits ETH by 70/10/5/5/10 basis points
- RevenueSplit.withdraw allows each role to claim accumulated share
- ReputationBadges.issueBadge mints NFT and stores evidence
- ReputationBadges.transferFrom always reverts
- SelfSustainingEcosystem supports all 6 roles (CREATOR, AUDITOR, REFERRER, DISPUTER, NODE, CURATOR)
- SelfSustainingEcosystem.generateHealthReport returns system metrics
- All tests pass with > 80% coverage
</success_criteria>

<output>
After completion, create `.planning/phases/22-healthreporter-integration/22-01-SUMMARY.md`
</output>