---
phase: 10-documentation-update
plan: "10-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILLS_STANDARD.md
autonomous: true
requirements:
  - DOCU-01
  - DOCU-02
user_setup: []

must_haves:
  truths:
    - "Section 6.4 no longer references 'future implementation'"
    - "Section 6.2 interface compatibility table reflects implemented functions"
    - "Version header shows v1.2"
    - "Version history table contains v1.2 entry"
  artifacts:
    - path: SKILLS_STANDARD.md
      provides: Updated specification with implemented recovery functions
      contains:
        - "version: 1.2" in header
        - "getRecoverableReputation()" documented
        - "claimRecoverableReputation()" documented
        - "setPositiveContribution()" documented
        - "ReputationLock struct" documented
        - "RECOVERY_RATE_PER_MONTH = 500 basis points" documented
        - v1.2 entry in version history table
---

<objective>
Update SKILLS_STANDARD.md to v1.2, reflecting implemented reputation recovery functions and lock mechanism from phases 8-9. Remove "future implementation" notes, update compatibility tables, document implemented functions with exact contract details.

Purpose: Documentation accurately reflects v1.2 implementation state
Output: SKILLS_STANDARD.md v1.2
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@SKILLS_STANDARD.md
@contracts/StakingManager.sol
@.planning/phases/08-recovery-functions/08-01-PLAN.md
@.planning/phases/09-01-PLAN.md
@.planning/phases/10-documentation-update/10-CONTEXT.md
</context>

<interfaces>
<!-- Key implementation details extracted from contracts for documentation -->

From contracts/StakingManager.sol (lines 23-39):

```solidity
/// 声望锁定结构（宪法第三条：反噬机制）
struct ReputationLock {
    uint256 lockedAmount;    // 当前锁定的声望金额
    uint256 lastClaimTime;   // 上次领取恢复的时间戳
}

/// 声望锁定映射（每用户）
mapping(address => ReputationLock) public reputationLocks;

/// 每月恢复比例：5% = 500 basis points
uint256 public constant RECOVERY_RATE_PER_MONTH = 500;
```

From contracts/StakingManager.sol (lines 150-162):

```solidity
/// @notice 获取用户可恢复的声望信息（RECOV-01）
/// @dev 返回锁定金额和上次领取时间
/// @param _user 用户地址
/// @return lockedAmount 当前锁定的声望金额
/// @return lastClaimTime 上次领取恢复的时间戳（如果是0表示从未被锁定）
function getRecoverableReputation(address _user)
    external
    view
    returns (uint256 lockedAmount, uint256 lastClaimTime)
{
    ReputationLock storage lock = reputationLocks[_user];
    return (lock.lockedAmount, lock.lastClaimTime);
}
```

From contracts/StakingManager.sol (lines 167-198):

```solidity
/// @notice 领取可恢复的声望（RECOV-02）
/// @dev 5% 每月恢复（基于原始惩罚金额），需有正面贡献
/// 要求：锁定金额 > 0，已有正面贡献，距上次领取 >= 1个月
function claimRecoverableReputation() external {
    // ... implementation details
    // 检查是否有锁定的声望
    require(lock.lockedAmount > 0, "No locked reputation");
    // 检查是否有正面贡献
    require(hasPositiveContribution[msg.sender], "No positive contribution");
    // 计算距上次领取的月份数
    uint256 monthsElapsed = (block.timestamp - lock.lastClaimTime) / 30 days;
    // 计算可恢复金额：originalSlash x 5% x months
    uint256 maxRecovery = (originalSlashAmount[msg.sender] * RECOVERY_RATE_PER_MONTH * monthsElapsed) / 10000;
}
```

From contracts/StakingManager.sol (lines 200-207):

```solidity
/// @notice 设置用户有正面贡献（由贡献合约主动调用）
/// @dev 幂等性检查：已设置则 revert
/// @param _user 用户地址
function setPositiveContribution(address _user) external onlyOwner {
    require(!hasPositiveContribution[_user], "Already set");
    hasPositiveContribution[_user] = true;
}
```

Key cross-contract wiring:
- Attribution.sol calls `StakingManager.setPositiveContribution(_contributor)` on positive attribution
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update version header to v1.2</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>SKILLS_STANDARD.md</read_first>
  <action>
Replace the header section (lines 1-8) with v1.2:

```
# AgentSkills Skills 标准规范 v1.2

**版本：** 1.2
**制定日期：** 2026-05-05
**状态：** 正式版 v1.2
**更新日期：** 2026-05-16
```

This implements D-01: Bump SKILLS_STANDARD.md to v1.2.
  </action>
  <acceptance_criteria>
- [ ] Header shows "版本：** 1.2"
- [ ] Header shows "状态：** 正式版 v1.2"
- [ ] Header shows "更新日期：** 2026-05-16"
- [ ] grep "1.2" SKILLS_STANDARD.md returns version header matches
  </acceptance_criteria>
  <verify>
<automated>grep -n "版本：.*1.2" SKILLS_STANDARD.md && grep -n "状态：.*正式版 v1.2" SKILLS_STANDARD.md && grep -n "更新日期：.*2026-05-16" SKILLS_STANDARD.md</automated>
  </verify>
  <done>Version header updated to v1.2 with current date</done>
</task>

<task type="auto">
  <name>Task 2: Update Section 6.2 interface compatibility table</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>SKILLS_STANDARD.md</read_first>
  <action>
Replace the interface compatibility table (lines 1189-1198) with updated entries:

Replace:
```
| getRecoverableReputation() | 不存在 | 当前版本无自动恢复机制 |
| claimRecoverableReputation() | 不存在 | 当前版本无领取机制 |
```

With:
```
| getRecoverableReputation() | `StakingManager.getRecoverableReputation(address _user)` | 返回 (lockedAmount, lastClaimTime) 元组 |
| claimRecoverableReputation() | `StakingManager.claimRecoverableReputation()` | 5% 每月恢复，需正面贡献，距上次领取 >= 1个月 |
| setPositiveContribution() | `StakingManager.setPositiveContribution(address _user)` | 仅 owner 可调用，幂等性检查 |
| ReputationLock struct | `StakingManager.ReputationLock { lockedAmount, lastClaimTime }` | 追踪每用户锁定状态 |
| RECOVERY_RATE_PER_MONTH | `StakingManager.RECOVERY_RATE_PER_MONTH = 500` | 500 basis points = 5% 每月 |
```

This implements D-03: Update interface compatibility table to show functions are now implemented.

Also remove the note on line 1198: "注意：当前合约实现采用简化的声望模型..."
  </action>
  <acceptance_criteria>
- [ ] Table shows "getRecoverableReputation()" with function signature
- [ ] Table shows "claimRecoverableReputation()" with implementation details
- [ ] Table shows "setPositiveContribution()" function
- [ ] Table shows "ReputationLock struct" with fields
- [ ] Table shows "RECOVERY_RATE_PER_MONTH = 500"
- [ ] Old "不存在" entries removed
- [ ] Old "当前版本无自动恢复机制" note removed
  </acceptance_criteria>
  <verify>
<automated>grep -n "getRecoverableReputation(address _user)" SKILLS_STANDARD.md && grep -n "claimRecoverableReputation()" SKILLS_STANDARD.md && grep -n "ReputationLock {" SKILLS_STANDARD.md && grep -n "RECOVERY_RATE_PER_MONTH = 500" SKILLS_STANDARD.md</automated>
  </verify>
  <done>Section 6.2 interface compatibility table updated with implemented functions</done>
</task>

<task type="auto">
  <name>Task 3: Replace Section 6.4 "future implementation" note with implementation documentation</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>SKILLS_STANDARD.md</read_first>
  <action>
Replace the "未来实现说明 (SLASH-03)" block at lines 1362-1363:

OLD (lines 1362-1363):
```
> **未来实现说明 (SLASH-03):** 本节描述的恢复机制 (`getRecoverableReputation()`、`claimRecoverableReputation()`) 和声望锁定功能为规划中的实现目标。当前合约版本使用简单的 `mapping(address => int256) public userReputation`，无自动恢复机制和锁定状态。详见 [6.2 反噬执行合约](#6-2-反噬执行合约) 的接口兼容性说明。
```

NEW:
```
> **实现状态 (v1.2):** 恢复机制已在 `StakingManager.sol` 中实现：
> - `getRecoverableReputation(address _user)` 返回 `(lockedAmount, lastClaimTime)`
> - `claimRecoverableReputation()` 执行 5% 每月恢复，需有正面贡献（D-03）
> - `ReputationLock` struct 追踪每用户锁定状态
> - 跨合约调用：正面归因时 `Attribution.sol` 调用 `setPositiveContribution()`
```

This implements D-02: Replace "future implementation" note with implementation notes.
  </action>
  <acceptance_criteria>
- [ ] Old "未来实现说明" text removed
- [ ] "实现状态 (v1.2)" label present
- [ ] getRecoverableReputation() documented
- [ ] claimRecoverableReputation() documented with 5% monthly rate
- [ ] ReputationLock struct referenced
- [ ] Cross-contract wiring (Attribution -> StakingManager) documented
  </acceptance_criteria>
  <verify>
<automated>grep -n "未来实现说明" SKILLS_STANDARD.md; echo "---"; grep -n "实现状态 (v1.2)" SKILLS_STANDARD.md</automated>
  </verify>
  <done>Section 6.4 "future implementation" note replaced with v1.2 implementation status</done>
</task>

<task type="auto">
  <name>Task 4: Update version history table with v1.2 entry</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>SKILLS_STANDARD.md</read_first>
  <action>
Add v1.2 entry to the version history table (Section 9.1, after line 1696):

Add before the closing `---`:
```
| v1.2 | 2026-05-16 | Phase 10: 实现声望恢复函数和锁定机制文档更新 |
```

This implements D-04: Add v1.2 entry to version history table.
  </action>
  <acceptance_criteria>
- [ ] Version history table contains "v1.2" entry
- [ ] Entry date shows "2026-05-16"
- [ ] Entry description mentions reputation recovery and lock mechanism documentation
  </acceptance_criteria>
  <verify>
<automated>grep -n "| v1.2 |" SKILLS_STANDARD.md</automated>
  </verify>
  <done>v1.2 entry added to version history table</done>
</task>

<task type="auto">
  <name>Task 5: Update document footer status</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>SKILLS_STANDARD.md</read_first>
  <action>
Update the document footer (line 1893-1894) to reflect v1.2:

OLD:
```
**文档状态**：正式版 v1.1
```

NEW:
```
**文档状态**：正式版 v1.2
```
  </action>
  <acceptance_criteria>
- [ ] Footer shows "正式版 v1.2"
- [ ] grep "正式版 v1.2" returns footer line
  </acceptance_criteria>
  <verify>
<automated>grep -n "正式版 v1.2" SKILLS_STANDARD.md</automated>
  </verify>
  <done>Document footer updated to v1.2</done>
</task>

</tasks>

<verification>
1. Verify version header: `grep -n "版本：.*1.2" SKILLS_STANDARD.md`
2. Verify no "未来实现说明" remains: `grep -n "未来实现说明" SKILLS_STANDARD.md` (should return nothing)
3. Verify implementation status: `grep -n "实现状态 (v1.2)" SKILLS_STANDARD.md`
4. Verify interface table updated: `grep -n "getRecoverableReputation(address _user)" SKILLS_STANDARD.md`
5. Verify version history: `grep -n "| v1.2 |" SKILLS_STANDARD.md`
6. Verify footer: `grep -n "正式版 v1.2" SKILLS_STANDARD.md`
</verification>

<success_criteria>
- [ ] Version header shows v1.2
- [ ] Section 6.2 interface compatibility table updated
- [ ] Section 6.4 "future implementation" note removed
- [ ] Implementation status (v1.2) label added
- [ ] Version history table contains v1.2 entry
- [ ] Document footer shows v1.2
- [ ] No "future implementation" references remain in document
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-update/10-01-SUMMARY.md`
</output>