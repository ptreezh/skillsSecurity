---
phase: "17"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/tokens.css
  - src/styles/components.css
  - src/components/SkillBrowser.jsx
  - src/pages/UserProfile.jsx
  - src/pages/Leaderboard.jsx
autonomous: true
must_haves:
  truths:
    - "CSS variables for all design tokens exist"
    - "Component styles in components.css"
    - "SkillBrowser updated with design tokens"
    - "UserProfile updated with design tokens"
    - "Leaderboard updated with design tokens"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "CSS custom properties for all design tokens"
      contains:
        - Colors (primary, secondary, semantic)
        - Risk level colors
        - Reputation tier colors
        - Spacing scale (8pt)
        - Typography scale
        - Border radius
        - Shadows
    - path: "src/styles/components.css"
      provides: "Reusable component classes"
      contains:
        - Button variants (primary, secondary, ghost, danger)
        - Card styles (skill, user, leaderboard row)
        - Input styles
        - Badge styles (risk, tier, verification)
        - Modal styles
---

<objective>
Create the CSS design system (tokens + components) and update existing pages to use the new design.
</objective>

<context>
@.planning/phases/17-frontend-ui-design/17-UI-SPEC.md
@src/main.jsx
@src/components/SkillBrowser.jsx
@src/pages/UserProfile.jsx
@src/pages/Leaderboard.jsx
</context>

<tasks>

<task>
<name>Task 01: Create tokens.css with all CSS custom properties</name>
<files>src/styles/tokens.css</files>
<action>
Create src/styles/tokens.css with all CSS variables from the UI spec:

```css
:root {
  /* === Brand Colors === */
  --color-primary: #2563eb;
  --color-primary-hover: #1d4ed8;
  --color-primary-light: #dbeafe;
  
  /* ... (all tokens from UI spec) */
}
```

Copy ALL CSS variables from section 2 of 17-UI-SPEC.md. Include:
- Brand colors (primary, secondary, accent)
- Semantic colors (success, warning, danger, info)
- Risk level colors (LOW, MEDIUM, HIGH, CRITICAL)
- Neutral colors (text, background, border)
- Spacing scale (space-1 through space-16)
- Border radius (sm, md, lg, xl, full)
- Shadow system (xs through xl)
- Typography (font stack, transitions)

Commit with message: "feat(17): add CSS design tokens"
</action>
<verify>
- [ ] tokens.css exists with 50+ CSS variables
- [ ] All color variables defined (primary, secondary, accent, semantic)
- [ ] Spacing scale complete (space-1 through space-16)
- [ ] Shadow system defined (xs through xl)
</verify>
</task>

<task>
<name>Task 02: Create components.css with reusable styles</name>
<files>src/styles/components.css</files>
<action>
Create src/styles/components.css with reusable component classes:

Include:
1. Buttons (.btn-primary, .btn-secondary, .btn-ghost, .btn-danger)
2. Cards (.card, .card-skill, .card-user, .card-leaderboard)
3. Inputs (.input, .input-search)
4. Badges (.badge-risk-low, .badge-risk-medium, .badge-risk-high, .badge-risk-critical, .badge-tier-1 through .badge-tier-5, .badge-verified)
5. Layout (.container, .grid, .flex, .sr-only)
6. Modal (.modal-overlay, .modal-content)

Import tokens.css at the top:
```css
@import './tokens.css';
```

Commit with message: "feat(17): add component CSS classes"
</action>
<verify>
- [ ] components.css exists
- [ ] @import tokens.css at top
- [ ] Button variants defined
- [ ] Card styles defined
- [ ] Badge styles defined
</verify>
</task>

<task>
<name>Task 03: Update SkillBrowser.jsx with design tokens</name>
<files>src/components/SkillBrowser.jsx</files>
<action>
Update SkillBrowser.jsx to use CSS classes instead of inline styles where practical.

Changes:
1. Add `import '../styles/components.css'` at the top
2. Replace inline card styles with className="card-skill"
3. Use .btn-primary for Like button
4. Use .badge-risk-* for risk badges
5. Keep functional inline styles (dynamic values from data)

Commit with message: "refactor(17): apply design tokens to SkillBrowser"
</action>
<verify>
- [ ] CSS classes imported
- [ ] Skill card uses card-skill class
- [ ] Like button uses btn-primary class
- [ ] Risk badges use badge-risk-* classes
</verify>
</task>

<task>
<name>Task 04: Update UserProfile.jsx with design tokens</name>
<files>src/pages/UserProfile.jsx</files>
<action>
Update UserProfile.jsx to use CSS classes:

Changes:
1. Add import for components.css
2. Replace inline styles with appropriate CSS classes
3. Use .card for profile card
4. Use .badge-tier-* for tier badges

Commit with message: "refactor(17): apply design tokens to UserProfile"
</action>
<verify>
- [ ] CSS classes imported
- [ ] Profile card uses card class
- [ ] Tier badges use badge-tier-* classes
</verify>
</task>

<task>
<name>Task 05: Update Leaderboard.jsx with design tokens</name>
<files>src/pages/Leaderboard.jsx</files>
<action>
Update Leaderboard.jsx to use CSS classes:

Changes:
1. Add import for components.css
2. Use .card for leaderboard container
3. Use .card-leaderboard for row styles
4. Apply badge-tier-* for user tier badges

Commit with message: "refactor(17): apply design tokens to Leaderboard"
</action>
<verify>
- [ ] CSS classes imported
- [ ] Leaderboard uses card classes
- [ ] Tier badges use badge-tier-* classes
</verify>
</task>

</tasks>

<verification>
1. Check src/styles/ directory has tokens.css and components.css
2. Verify all pages import components.css
3. Run `npm run dev` to ensure no syntax errors
</verification>

<success_criteria>
- All CSS design tokens defined in tokens.css
- Component classes available in components.css
- All 3 pages (SkillBrowser, UserProfile, Leaderboard) updated with design tokens
- No inline style conflicts with new classes
- Development server starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-frontend-ui-design/17-01-SUMMARY.md`
</output>