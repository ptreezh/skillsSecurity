---
phase: 02-classification-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILLS_STANDARD.md
autonomous: true
requirements:
  - CLASS-01
  - CLASS-02
  - CLASS-03
  - CLASS-04

must_haves:
  truths:
    - Section 2.1 table shows correct staking amounts (10/50/100/200 ether)
    - Each risk level has at least 5 concrete examples
    - AI-generated code classification rules are explicit
    - Decision tree guides creators through risk assessment
  artifacts:
    - path: SKILLS_STANDARD.md
      provides: Classification standards with updated stake amounts, expanded examples, AI rules, decision tree
      contains: MIN_STAKE table, 5+ examples per level, AI classification rules, ASCII decision tree
  key_links:
    - from: Section 2.1 table
      to: Section 2.3 Solidity constants
      via: Same values (10/50/100/200)
---

<objective>
Update Section 2 (技能分类标准) with expanded examples, AI code classification rules, a risk assessment decision tree, and correct staking amounts from contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@SKILLS_STANDARD.md (lines 75-128, Section 2)
@contracts/SkillRegistry.sol (lines 41-44, MIN_STAKE_* constants)
@.planning/phases/02-classification-standards/02-CONTEXT.md (user decisions)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Section 2.1 stake amounts and expand examples</name>
  <files>SKILLS_STANDARD.md</files>
  <behavior>
    - Section 2.1 table updated: LOW=10, MEDIUM=50, HIGH=100, CRITICAL=200 (matching CLASS-04)
    - Each risk level has 5+ concrete examples (per CLASS-01)
    - Existing risk criteria descriptions preserved
  </behavior>
  <action>
Update the risk等级定义 table in Section 2.1:

1. Change "积分要求" column values to match contract MIN_STAKE_*:
   - LOW: 10
   - MEDIUM: 50
   - HIGH: 100
   - CRITICAL: 200

2. Expand "示例技能" column for each risk level to minimum 5 examples:

LOW (低风险):
Current: 日历查询、邮件发送、天气查询
New: 日历查询、邮件发送、天气查询、股票数据获取、新闻聚合、汇率查询、地图位置查询、字典/翻译

MEDIUM (中风险):
Current: 文件操作、API 调用、数据抓取
New: 文件操作、API 调用、数据抓取、发送邮件、创建文件、更新配置、社交媒体发帖、预约管理

HIGH (高风险):
Current: 支付转账、消息发送、数据修改
New: 支付转账、消息发送、数据修改、删除文件、修改权限、批量操作、第三方支付集成、敏感数据导出

CRITICAL (极高风险):
Current: 资金操作、权限变更、系统管理
New: 资金操作、权限变更、系统管理、智能合约调用、数据库管理、用户权限授予、API密钥管理、批量用户管理

Keep the verification thresholds (verifier count, reputation) as they are.
  </action>
  <verify>
    <automated>grep -c "示例技能" SKILLS_STANDARD.md && grep -E "10.*50.*100.*200" SKILLS_STANDARD.md</automated>
  </verify>
  <done>Table shows 10/50/100/200 and each risk level has 5+ examples</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add AI-generated code classification rules to Section 2.2</name>
  <files>SKILLS_STANDARD.md</files>
  <behavior>
    - New "AI生成代码分类" sub-section added after existing risk criteria
    - Rules for >50% AI code requiring elevated classification
    - Boundary defined for AI-assisted vs AI-autonomous
  </behavior>
  <action>
After the existing CRITICAL description in Section 2.2, add a new sub-section titled "### AI 生成代码分类规则 {#2-2-ai-code-classification}" with the following content:

```markdown
### AI 生成代码分类规则 {#2-2-ai-code-classification}

AI生成代码比例分类：

| AI 代码占比 | 分类级别 | 说明 |
|------------|----------|------|
| <20% | 同等风险 | 视为人类编写代码 |
| 20-50% | AI 辅助 | 轻微提升验证要求 |
| >50% | 需提升等级 | 至少提升一个风险等级 |

分类规则：

1. **提升原则**：当技能的 AI 生成代码超过 50% 时，该技能应至少比同类人工编写技能高一个风险等级。

2. **AI 辅助判定**：AI 生成代码占 20-50% 时，需要额外提交 AI 工具链清单，包括使用的模型版本和提示词模板。

3. **AI 自主判定**：AI 生成代码超过 80% 时，视为 AI 自主生成，需要最高等级验证（CRITICAL），且需要人工审核每行代码逻辑。

4. **验证要求提升**：
   - AI 辅助 (20-50%): 验证时间加倍
   - AI 主导 (>50%): 提升一级风险等级
   - AI 自主 (>80%): 按 CRITICAL 处理

5. **披露义务**：创建者必须在技能描述中明确标注 AI 代码占比，未披露者视为违规。
```

Insert this after line 114 (end of CRITICAL description), before the "---" separator.
  </action>
  <verify>
    <automated>grep -A5 "AI 生成代码分类规则" SKILLS_STANDARD.md</automated>
  </verify>
  <done>AI classification rules exist with >50% threshold and escalation logic</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add risk assessment decision tree to Section 2.2</name>
  <files>SKILLS_STANDARD.md</files>
  <behavior>
    - ASCII decision tree flowchart added as new sub-section
    - Tree guides through read/write, reversibility, fund exposure, data impact
    - Matches existing document style (ASCII diagrams)
  </behavior>
  <action>
After the AI classification rules sub-section, add a new sub-section titled "### 风险分类决策树 {#2-2-decision-tree}" with the following ASCII flowchart:

```markdown
### 风险分类决策树 {#2-2-decision-tree}

使用以下决策树确定技能风险等级：

```
                        ┌─────────────────────────┐
                        │      开始评估            │
                        │   技能是否可以执行？      │
                        └───────────┬─────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
                 [ 否 ]                           [ 是 ]
                    │                               │
                    ▼                               ▼
           ┌───────────────┐          ┌─────────────────────────┐
           │   不需要上链    │          │   步骤 1：是否修改外部状态？  │
           │   (纯本地执行)  │          └────────────┬────────────┘
           └───────────────┘                         │
                                        ┌────────────┴────────────┐
                                        │                         │
                                        ▼                         ▼
                                     [ 否 ]                    [ 是 ]
                                        │                         │
                                        ▼                         ▼
                              ┌─────────────────┐    ┌─────────────────────────┐
                              │  步骤 2：是否涉及  │    │       风险等级升级       │
                              │  可逆操作？        │    │   进入 [步骤 2]          │
                              └─────────┬───────┘    └─────────────────────────┘
                                        │
                        ┌───────────────┴───────────────┐
                        │                               │
                        ▼                               ▼
                     [ 是 ]                         [ 否 ]
                        │                               │
                        ▼                               ▼
              ┌─────────────────┐         ┌─────────────────────────┐
              │      LOW        │         │  步骤 3：是否涉及资金？   │
              │   (低风险)       │         └────────────┬────────────┘
              └─────────────────┘                      │
                                        ┌────────────┴────────────┐
                                        │                         │
                                        ▼                         ▼
                                     [ 是 ]                    [ 否 ]
                                        │                         │
                                        ▼                         ▼
                              ┌─────────────────┐    ┌─────────────────────────┐
                              │      HIGH       │    │  步骤 4：是否暴露用户数据？│
                              │   (高风险)       │    └────────────┬────────────┘
                              └─────────────────┘                      │
                                        ┌────────────┴────────────┐
                                        │                         │
                                        ▼                         ▼
                                     [ 是 ]                    [ 否 ]
                                        │                         │
                                        ▼                         ▼
                              ┌─────────────────┐         ┌─────────────────┐
                              │    CRITICAL     │         │     MEDIUM      │
                              │   (极高风险)     │         │    (中风险)      │
                              └─────────────────┘         └─────────────────┘

补充规则：
┌─────────────────────────────────────────────────────────────────┐
│  1. AI 代码 >50%：自动提升一个等级（即使流程判定为 LOW）           │
│  2. 涉及跨平台操作：自动提升一个等级                               │
│  3. 处理金融数据：至少 MEDIUM                                    │
│  4. 修改系统权限：直接 CRITICAL                                   │
└─────────────────────────────────────────────────────────────────┘

快速判断：
┌─────────────────────────────────────────────────────────────────┐
│  ✅ 只读查询              → LOW                                    │
│  ✅ 用户可控文件操作      → MEDIUM                                  │
│  🔴 他人数据/资金操作     → HIGH                                   │
│  🚨 系统权限/智能合约     → CRITICAL                                │
└─────────────────────────────────────────────────────────────────┘
```
```

Insert this after the AI classification rules section.
  </action>
  <verify>
    <automated>grep -c "决策树" SKILLS_STANDARD.md && grep -c "开始评估" SKILLS_STANDARD.md</automated>
  </verify>
  <done>ASCII decision tree exists with all four decision points (read/write, reversibility, fund, data)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Verify Section 2.3 Solidity constants align with updated table</name>
  <files>SKILLS_STANDARD.md</files>
  <behavior>
    - Section 2.3 shows MIN_STAKE values: LOW=10, MEDIUM=50, HIGH=100, CRITICAL=200
    - Values match both contract (lines 41-44) and updated table in Section 2.1
  </behavior>
  <action>
Read Section 2.3 (Solidity 实现, lines 116-127) and verify the MIN_STAKE constants match the updated table values:

Current values in document (should be already correct):
```solidity
uint256 public constant MIN_STAKE_LOW = 10 ether;
uint256 public constant MIN_STAKE_MEDIUM = 50 ether;
uint256 public constant MIN_STAKE_HIGH = 100 ether;
uint256 public constant MIN_STAKE_CRITICAL = 200 ether;
```

If document already shows these values (from the read, it does), no change needed. Confirm alignment by adding a comment referencing the table:

```solidity
// 风险等级常量 (与 2.1 节表格保持一致)
uint256 public constant MIN_STAKE_LOW = 10 ether;
uint256 public constant MIN_STAKE_MEDIUM = 50 ether;
uint256 public constant MIN_STAKE_HIGH = 100 ether;
uint256 public constant MIN_STAKE_CRITICAL = 200 ether;
```

If any value differs from the contract (10/50/100/200), update to match.
  </action>
  <verify>
    <automated>grep -A3 "MIN_STAKE_LOW" SKILLS_STANDARD.md</automated>
  </verify>
  <done>Section 2.3 constants show 10/50/100/200 and align with Section 2.1 table</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is documentation only - no trust boundaries involved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | N/A | SKILLS_STANDARD.md | N/A | Documentation update, no security implications |
</threat_model>

<verification>
- [ ] Section 2.1 table updated with 10/50/100/200 values
- [ ] Each risk level has 5+ examples
- [ ] AI classification rules section exists with >50% threshold
- [ ] Decision tree ASCII flowchart exists with 4 decision points
- [ ] Section 2.3 constants match contract values
</verification>

<success_criteria>
Section 2 (技能分类标准) complete with:
- Correct staking amounts (CLASS-04: MIN_STAKE_* aligned)
- 5+ examples per risk level (CLASS-01)
- AI-generated code rules (CLASS-02)
- Risk assessment decision tree (CLASS-03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-classification-standards/02-01-SUMMARY.md`
</output>