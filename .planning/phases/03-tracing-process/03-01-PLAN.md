---
phase: "03-tracing-process"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILLS_STANDARD.md
autonomous: true
requirements:
  - TRACE-01
  - TRACE-02
  - TRACE-03
  - TRACE-04
  - PROC-01
  - PROC-02
  - PROC-03
  - PROC-04

must_haves:
  truths:
    - "Fingerprint algorithm is fully documented with inputs, outputs, and usage"
    - "Minimum 8 audit trail events are defined with format and purpose"
    - "IPFS storage format specifies on-chain/off-chain separation"
    - "State machine includes all core states and sub-states with transitions"
    - "Error codes cover all failure modes with module prefixes"
    - "Gas cost estimates exist for key operations"
    - "Appeal process includes timeline and committee structure"
  artifacts:
    - path: "SKILLS_STANDARD.md"
      provides: "Complete Sections 4 and 5"
      sections:
        - "4.1 指纹机制 (expanded)"
        - "4.3 审计追踪 (complete)"
        - "4.4 IPFS存储格式 (new)"
        - "4.5 追溯验证测试 (new)"
        - "5.3 状态机 (complete)"
        - "5.4 错误码 (new)"
        - "5.5 Gas成本估算 (new)"
        - "6.3 申诉流程 (expanded)"
---

<objective>
Complete Section 4 (责任追溯标准) and Section 5 (上链流程标准) in SKILLS_STANDARD.md with all TRACE and PROC requirements.

Purpose: Document traceability mechanisms and on-chain processes to ensure full accountability chain.
Output: Complete Sections 4 and 5 with fingerprint algorithm, audit trail, IPFS format, state machine, error codes, gas estimates, and appeal process.
</objective>

<context>
@SKILLS_STANDARD.md
@.planning/phases/03-tracing-process/03-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Section 4 - Tracing Standards (TRACE-01, TRACE-02, TRACE-03, TRACE-04)</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>
    - SKILLS_STANDARD.md (lines 459-516 - current Section 4)
    - 03-CONTEXT.md (lines 26-43 - audit trail events and IPFS decisions)
  </read_first>
  <action>
## Task 1: Expand Section 4 - Tracing Standards

### 1.1 Expand 4.1 指纹机制 (TRACE-01)

Replace current 4.1 content with expanded version:

```markdown
### 4.1 指纹机制 {#4-1-指纹机制}

每个技能创建时生成唯一指纹，用于追溯和验证完整性：

#### 算法定义

指纹计算公式：
```
Fingerprint = keccak256(IPFS_Hash || Creator_Address || Timestamp)
```

| 参数 | 类型 | 说明 | 存储 |
|------|------|------|------|
| IPFS_Hash | bytes | IPFS内容地址 | 链上 |
| Creator_Address | address | 创建者钱包地址 | 链上 |
| Timestamp | uint256 | Unix时间戳（秒） | 链上 |

#### Solidity 实现

```solidity
// 指纹计算
function computeFingerprint(
    string memory _ipfsHash,   // IPFS 存储地址
    address _creator,           // 创建者地址
    uint256 _timestamp         // 创建时间戳
) public pure returns (bytes32) {
    return keccak256(abi.encodePacked(_ipfsHash, _creator, _timestamp));
}

// 查询指纹
function getFingerprint(uint256 _skillId)
    external view returns (bytes32) {
    return skills[_skillId].fingerprint;
}
```

#### 指纹用途

| 用途 | 说明 |
|------|------|
| 身份验证 | 验证技能代码未被篡改 |
| 溯源追踪 | 追踪技能创建者和时间 |
| 完整性校验 | 比对链上指纹与IPFS内容 |
| 争议解决 | 作为证据用于申诉流程 |

#### 指纹生成时机

指纹在 `registerSkill()` 调用时自动生成，存储在 Skill 结构体的 `fingerprint` 字段。
```

### 1.2 Create 4.4 IPFS存储格式 (TRACE-03)

Add new subsection after 4.3:

```markdown
### 4.4 IPFS存储格式 {#4-4-ipfs存储格式}

采用混合存储策略：关键数据上链，可验证数据存IPFS。

#### 混合存储架构

```
┌─────────────────────────────────────────────────────────────┐
│                    混合存储架构                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   链上存储（高效查询）          IPFS存储（完整验证）            │
│   ┌─────────────────┐       ┌─────────────────┐            │
│   │ • skillId       │       │ • 完整代码      │            │
│   │ • fingerprint   │       │ • 完整文档      │            │
│   │ • creator       │       │ • 审计报告      │            │
│   │ • riskLevel     │       │ • 验证证据      │            │
│   │ • status        │       │ • 版本历史      │            │
│   │ • timestamps    │       │                 │            │
│   └─────────────────┘       └─────────────────┘            │
│                                                             │
│   链接：metadataIPFS字段指向IPFS内容                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### IPFS元数据结构

所有上传到IPFS的内容必须包含标准元数据头：

```json
{
  "schema_version": "1.0",
  "content_type": "skill_code|skill_doc|audit_report|verification_evidence",
  "signature": "0x...",
  "signed_by": "0x...",
  "timestamp": 1714896000,
  "content_hash": "QmXxx...",
  "previous_hash": "QmYyy...",
  "metadata": {
    "skillId": 1,
    "version": "1.0.0",
    "riskLevel": 1
  }
}
```

#### 元数据字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| schema_version | string | 是 | 遵循的元数据Schema版本 |
| content_type | enum | 是 | 内容类型：skill_code/skill_doc/audit_report/verification_evidence |
| signature | bytes | 是 | 创建者签名（签名内容为content_hash） |
| signed_by | address | 是 | 签名者钱包地址 |
| timestamp | uint256 | 是 | Unix时间戳 |
| content_hash | string | 是 | IPFS内容哈希 |
| previous_hash | string | 否 | 前版本内容哈希（用于版本链） |
| metadata | object | 是 | 关联的技能元数据 |

#### 内容类型规范

**skill_code（技能代码）:**
- 包含完整源代码
- 包含 package.json 或等效依赖声明
- 包含 README.md

**skill_doc（技能文档）:**
- 包含使用说明
- 包含API文档
- 包含示例代码

**audit_report（审计报告）:**
- 包含审计员签名
- 包含漏洞列表
- 包含修复建议

**verification_evidence（验证证据）:**
- 包含验证者意见
- 包含安全扫描报告
- 包含投票结果
```

### 1.3 Create 4.5 追溯验证测试 (TRACE-04)

Add new subsection after 4.4:

```markdown
### 4.5 追溯验证测试 {#4-5-追溯验证测试}

为确保追溯机制有效性，定义以下验证测试用例：

#### 4.5.1 指纹验证测试

| 测试ID | 测试名称 | 输入 | 预期结果 |
|--------|----------|------|----------|
| TV-01 | 指纹生成 | IPFS_Hash, Creator, Timestamp | 返回32字节哈希 |
| TV-02 | 指纹唯一性 | 不同输入组合 | 产生不同指纹 |
| TV-03 | 指纹一致性 | 相同技能多次查询 | 返回相同指纹 |
| TV-04 | 指纹验证 | 链上指纹 + IPFS内容 | 验证通过/失败 |

#### 4.5.2 审计追踪验证测试

| 测试ID | 测试名称 | 验证点 |
|--------|----------|--------|
| TV-05 | 事件完整性 | 所有8个最小事件均被记录 |
| TV-06 | 事件顺序 | 事件按时间顺序排列 |
| TV-07 | 事件不可篡改 | 审计记录仅追加 |
| TV-08 | 事件可查询 | 可按skillId查询完整记录 |

#### 4.5.3 IPFS链接验证测试

| 测试ID | 测试名称 | 验证点 |
|--------|----------|--------|
| TV-09 | 元数据完整性 | 上传内容包含标准元数据 |
| TV-10 | 签名有效性 | 签名可被creator地址验证 |
| TV-11 | 版本链验证 | 可追溯内容历史版本 |
| TV-12 | 内容完整性 | IPFS内容哈希匹配元数据 |

#### 4.5.4 集成验证测试

| 测试ID | 测试名称 | 验证场景 |
|--------|----------|----------|
| TV-13 | 完整溯源 | 从调用记录可追溯到创建者 |
| TV-14 | 争议解决 | 指纹+审计记录可用于申诉 |
| TV-15 | 跨版本追踪 | 版本更新后仍可追溯历史 |

#### 测试执行要求

- TV-01 到 TV-04: 每次技能注册时执行
- TV-05 到 TV-08: 每次状态变更时执行
- TV-09 到 TV-12: 上传IPFS内容时执行
- TV-13 到 TV-15: 定期批量执行
```
  </action>
  <verify>
    <automated>grep -c "### 4.1 指纹机制" SKILLS_STANDARD.md && grep -c "### 4.4 IPFS存储格式" SKILLS_STANDARD.md && grep -c "### 4.5 追溯验证测试" SKILLS_STANDARD.md && grep -c "keccak256(abi.encodePacked" SKILLS_STANDARD.md</automated>
  </verify>
  <done>
    Section 4 complete with:
    - 4.1 指纹机制 expanded with algorithm, inputs/outputs, usage, timing
    - 4.3 审计追踪 updated with 8 minimum audit events in table format
    - 4.4 IPFS存储格式 new subsection with hybrid storage architecture
    - 4.5 追溯验证测试 new subsection with 15 test cases (TV-01 to TV-15)
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand Section 5 and 6.3 - Process Standards (PROC-01, PROC-02, PROC-03, PROC-04)</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>
    - SKILLS_STANDARD.md (lines 518-670 - current Sections 5 and 6.3)
    - 03-CONTEXT.md (lines 45-89 - state machine, error codes, gas estimates, appeal decisions)
  </read_first>
  <action>
## Task 2: Expand Section 5 and 6.3 - Process Standards

### 2.1 Expand 5.3 状态机 (PROC-01)

Replace current 5.3 content with complete state machine:

```markdown
### 5.3 状态机 {#5-3-状态机}

技能在合约中遵循定义良好的状态机，确保流程可控且可追溯。

#### 核心状态定义

| 状态 | 代码 | 说明 | 进入条件 | 退出条件 |
|------|------|------|----------|----------|
| DRAFT | 0 | 草稿状态 | 默认状态 | submit() |
| PENDING | 1 | 等待验证 | submit()完成 | assignVerifiers() |
| VERIFYING | 2 | 验证中 | verifiers已分配 | 投票完成 |
| APPROVED | 3 | 已批准 | 2/3+同意 | activate() |
| ACTIVE | 4 | 已激活 | APPROVED后 | slash() |
| REJECTED | 5 | 已拒绝 | 2/3+拒绝 | 可重新提交 |
| SLAUGHTERED | 6 | 已惩罚 | 触发反噬 | 申诉或结束 |

#### 子状态定义

**VERIFYING 子状态:**

| 子状态 | 代码 | 说明 | 进入条件 | 退出条件 |
|--------|------|------|----------|----------|
| VERIFYING_WAITING | 2.1 | 等待分配 | 状态变为VERIFYING | verifiers已分配 |
| VERIFYING_IN_PROGRESS | 2.2 | 投票进行中 | verifiers已分配 | 超时或投票完成 |
| VERIFYING_EXPIRED | 2.3 | 验证超时 | 超时阈值 | 重新分配或拒绝 |

**APPROVED 子状态:**

| 子状态 | 代码 | 说明 | 进入条件 | 退出条件 |
|--------|------|------|----------|----------|
| APPROVED_PENDING_CHAIN | 3.1 | 等待上链 | approve()完成 | 交易发送 |
| APPROVED_CHAINING | 3.2 | 上链进行中 | 交易已发送 | 链上确认 |
| APPROVED_FAILED | 3.3 | 上链失败 | 交易失败 | 重试或取消 |

**特殊状态:**

| 状态 | 代码 | 说明 |
|------|------|------|
| UNDER_APPEAL | 7 | 申诉中（可与其他状态叠加） |

#### 完整状态流转图

```
                              ┌──────────────────┐
                              │      DRAFT        │
                              │     (草稿)        │
                              │   status = 0      │
                              └────────┬─────────┘
                                       │ submit()
                                       ▼
                              ┌──────────────────┐
                              │     PENDING      │
                              │   (等待验证)      │
                              │   status = 1      │
                              └────────┬─────────┘
                                       │ assignVerifiers()
                                       ▼
                              ┌──────────────────┐
                              │    VERIFYING      │
                              │    (验证中)       │
                              │   status = 2      │
                              └────────┬─────────┘
                                       │
               ┌───────────────────────┼───────────────────────┐
               │                       │                       │
               ▼                       ▼                       ▼
      ┌────────────────┐      ┌────────────────┐      ┌────────────────┐
      │VERIFYING_WAITING│      │VERIFYING_IN_   │      │VERIFYING_      │
      │   (等待分配)     │      │PROGRESS        │      │EXPIRED         │
      │  subStatus=2.1  │      │  (投票中)       │      │  (已超时)       │
      └───────┬────────┘      │  subStatus=2.2  │      │  subStatus=2.3  │
              │               └───────┬────────┘      └────────────────┘
              │ assignVerifiers()     │                          │
              └───────────────────────┘                     assignVerifiers()
                              │                                   │
                              └───────────────────────────────────┘
                                       │
                        ┌──────────────┴──────────────┐
                        │                             │
                        ▼                             ▼
               ┌────────────────┐           ┌────────────────┐
               │   APPROVED     │           │   REJECTED     │
               │   (已批准)      │           │   (已拒绝)      │
               │  status = 3    │           │  status = 5    │
               └────────┬───────┘           └────────────────┘
                        │ approve()
                        ▼
               ┌────────────────┐
               │  UNDER_APPEAL  │◄── 申诉入口
               │   (申诉中)      │
               │  status = 7    │
               └────────┬───────┘
                        │
         ┌──────────────┼──────────────┐
         │              │              │
         ▼              ▼              ▼
┌────────────┐  ┌────────────┐  ┌────────────┐
│APPROVED_   │  │APPROVED_   │  │  ACTIVE    │
│PENDING_CHAIN│  │CHAINING   │  │  (已激活)   │
│(待上链)    │  │ (上链中)  │  │status = 4  │
│subStatus=3.1│  │subStatus=3.2│  └─────┬─────┘
└──────┬─────┘  └──────┬─────┘        │
       │ activate()   │ confirm()     │ slash()
       ▼               ▼              ▼
┌────────────┐  ┌────────────┐  ┌────────────────┐
│APPROVED_   │  │APPROVED_   │  │   SLAUGHTERED  │
│CHAINING   │  │FAILED      │  │   (已惩罚)     │
│subStatus=3.2│  │subStatus=3.3│  │  status = 6   │
└────────────┘  └────────────┘  └────────────────┘
                                          │
                                    UNDER_APPEAL ◄──┘
```

#### 状态转换触发条件

| 当前状态 | 目标状态 | 触发函数 | 触发条件 |
|----------|----------|----------|----------|
| DRAFT | PENDING | submit() | 提交信息完整，积分充足 |
| PENDING | VERIFYING | assignVerifiers() | verifier数量满足要求 |
| VERIFYING | APPROVED | approve() | 2/3+验证者同意 |
| VERIFYING | REJECTED | reject() | 超过阈值拒绝 |
| VERIFYING | VERIFYING_EXPIRED | (自动) | 超时阈值到达 |
| APPROVED | ACTIVE | activate() | 链上确认成功 |
| APPROVED | UNDER_APPEAL | fileAppeal() | 申诉窗口内 |
| ACTIVE | SLAUGHTERED | slash() | 触发反噬条件 |
| SLAUGHTERED | APPROVED | (申诉通过) | 申诉通过撤销惩罚 |
| SLAUGHTERED | ACTIVE | (申诉通过) | 申诉通过恢复活性 |
| REJECTED | DRAFT | resubmit() | 修改后重新提交 |

#### 状态查询接口

```solidity
function getSkillState(uint256 _skillId) 
    external view returns (State state, uint8 subState);
```
```

### 2.2 Create 5.4 错误码 (PROC-02)

Add new subsection after 5.3:

```markdown
### 5.4 错误码定义 {#5-4-错误码定义}

每个操作失败时返回标准错误码，便于诊断和调试。

#### 错误码格式

```
MODULE_NUMBER
```
- MODULE: 模块前缀（3字符）
- NUMBER: 错误编号（3位数字）

#### 模块前缀定义

| 模块 | 说明 | 覆盖范围 |
|------|------|----------|
| REG | Registration | 技能注册相关 |
| VAL | Validation | 验证流程相关 |
| CHN | Chain | 上链操作相关 |
| ATR | Attribution | 贡献归因相关 |
| STK | Staking | 质押管理相关 |
| REP | Reputation | 声望系统相关 |

#### 错误码表

**REG 模块（技能注册）:**

| 错误码 | 描述 | 建议操作 | 可重试 |
|--------|------|----------|--------|
| REG_001 | 技能名称已被占用 | 使用其他名称 | 是 |
| REG_002 | 积分不足 | 增加积分后重试 | 是 |
| REG_003 | 风险等级无效 | 检查输入值 | 否 |
| REG_004 | IPFS哈希无效 | 验证IPFS上传 | 是 |
| REG_005 | 触发词格式错误 | 遵循触发词规范 | 否 |
| REG_006 | 描述长度超限 | 缩短描述 | 否 |
| REG_007 | 版本格式错误 | 使用语义化版本 | 否 |

**VAL 模块（验证流程）:**

| 错误码 | 描述 | 建议操作 | 可重试 |
|--------|------|----------|--------|
| VAL_001 | 验证者声望不足 | 积累声望后重试 | 是 |
| VAL_002 | 验证者数量不足 | 等待更多验证者 | 是 |
| VAL_003 | 投票已结束 | 无需操作 | 否 |
| VAL_004 | 已投过票 | 无需操作 | 否 |
| VAL_005 | 验证超时 | 重新分配verifiers | 是 |
| VAL_006 | 利益冲突 | 验证者回避 | 否 |
| VAL_007 | 证据不足 | 补充证据 | 是 |

**CHN 模块（上链操作）:**

| 错误码 | 描述 | 建议操作 | 可重试 |
|--------|------|----------|--------|
| CHN_001 | 链上交易失败 | 检查网络状况 | 是 |
| CHN_002 | gas估算不足 | 增加gas limit | 是 |
| CHN_003 | 状态不允许上链 | 检查状态机 | 否 |
| CHN_004 | 签名验证失败 | 重新签名 | 是 |
| CHN_005 | 确认超时 | 等待确认后重试 | 是 |

**ATR 模块（贡献归因）:**

| 错误码 | 描述 | 建议操作 | 可重试 |
|--------|------|----------|--------|
| ATR_001 | 技能不存在 | 检查skillId | 否 |
| ATR_002 | 贡献者不是owner | 确认权限 | 否 |
| ATR_003 | 份额总和超过100% | 调整份额 | 否 |
| ATR_004 | 重复贡献 | 更新现有贡献 | 否 |

**STK 模块（质押管理）:**

| 错误码 | 描述 | 建议操作 | 可重试 |
|--------|------|----------|--------|
| STK_001 | 质押金额不足 | 增加质押 | 是 |
| STK_002 | 质押已锁定 | 等待解锁期 | 否 |
| STK_003 | 质押不存在 | 检查质押记录 | 否 |
| STK_004 | 解锁时间未到 | 等待解锁期 | 否 |
| STK_005 | 惩罚金额超限 | 确认惩罚金额 | 否 |

**REP 模块（声望系统）:**

| 错误码 | 描述 | 建议操作 | 可重试 |
|--------|------|----------|--------|
| REP_001 | 声望不足 | 积累声望 | 是 |
| REP_002 | 声望冻结 | 等待解冻 | 否 |
| REP_003 | 声望变化超出范围 | 检查变化值 | 否 |
| REP_004 | 权限不足 | 提升声望 | 是 |

#### 错误响应格式

```json
{
  "success": false,
  "error": {
    "code": "REG_001",
    "module": "REG",
    "number": "001",
    "description": "技能名称已被占用",
    "suggestion": "使用其他名称",
    "retryable": true
  }
}
```
```

### 2.3 Create 5.5 Gas成本估算 (PROC-03)

Add new subsection after 5.4:

```markdown
### 5.5 Gas成本估算 {#5-5-gas成本估算}

以下估算基于当前合约实现，实际gas消耗可能因网络状况而异。

#### 主要操作Gas估算

| 操作 | 函数 | 估算Gas | 说明 |
|------|------|---------|------|
| 注册技能（LOW） | registerSkill() | ~150,000 | 含质押转账 |
| 注册技能（MEDIUM） | registerSkill() | ~150,000 | 含质押转账 |
| 注册技能（HIGH） | registerSkill() | ~150,000 | 含质押转账 |
| 注册技能（CRITICAL） | registerSkill() | ~150,000 | 含质押转账 |
| 分配验证者 | assignVerifiers() | ~100,000 | 状态更新 |
| 验证投票 | verifySkill() | ~80,000 | 投票记录 |
| 技能激活 | activateSkill() | ~120,000 | 状态变更+事件 |
| 技能惩罚 | slashSkill() | ~90,000 | 质押扣减 |

#### 各风险等级总Gas消耗

| 风险等级 | 注册→激活总Gas | 估算成本（$0.1/gas） |
|----------|---------------|---------------------|
| LOW | ~450,000 | ~$45 |
| MEDIUM | ~450,000 | ~$45 |
| HIGH | ~450,000 | ~$45 |
| CRITICAL | ~450,000 | ~$45 |

注：成本估算基于Polygon网络（~$0.1/gas），以太坊主网成本约为100倍。

#### Gas优化建议

1. **批量验证**: 多个技能一起验证可分摊固定成本
2. **签名聚合**: 聚合验证者签名减少链上操作
3. **IPFS批量上传**: 代码和文档分开上传效率更高

#### 实际测量方法

使用Hardhat Gas Reporter测量实际gas消耗：

```bash
npx hardhat test --grep "gas"
```
```

### 2.4 Expand 6.3 申诉流程 (PROC-04)

Replace current 6.3 content with expanded version:

```markdown
### 6.3 申诉流程 {#6-3-申诉流程}

当用户对惩罚或决策有异议时，可通过申诉流程请求复核。

#### 申诉条件

以下情况可提出申诉：

| 违规类型 | 申诉条件 | 申诉期限 |
|----------|----------|----------|
| 技能惩罚（已造成损害） | 有新证据证明非故意 | 7天 |
| 技能惩罚（未造成损害） | 有新证据证明善意 | 7天 |
| 验证者惩罚 | 有证据证明误判 | 7天 |
| 验证者惩罚（重复误判） | 有证据证明非故意 | 7天 |
| 声望冻结 | 有证据证明误判 | 7天 |

以下情况**不可**申诉：
- 虚假举报被惩罚（无申诉窗口）
- 永久取消验证资格
- 恶意行为证据确凿

#### 申诉时间线

```
Day 0                Day 1-7             Day 8-10           Day 11
  │                     │                   │                  │
  ▼                     ▼                   ▼                  ▼
┌─────────┐      ┌─────────────┐    ┌────────────┐    ┌────────────┐
│惩罚通知 │ ───▶ │提交申诉材料 │ ──▶ │委员会审核  │ ──▶ │  裁决发布  │
│收到通知 │      │ 7天内提交   │    │3名长老复审 │    │三种结果   │
└─────────┘      └─────────────┘    └────────────┘    └────────────┘
                                                                  │
                              ┌─────────────────┬─────────────────┘
                              ▼                 ▼                 ▼
                        ┌──────────┐     ┌──────────┐      ┌──────────┐
                        │ 维持惩罚  │     │ 撤销惩罚  │      │ 部分撤销  │
                        │(原判生效) │     │(完全恢复) │      │(减轻惩罚) │
                        └──────────┘     └──────────┘      └──────────┘
```

#### 申诉材料要求

**必须提交:**
- [ ] 原始惩罚通知副本
- [ ] 申诉理由陈述（不超过1000字）
- [ ] 时间线说明
- [ ] 反驳证据（如有）

**可选提交:**
- [ ] 第三方安全评估报告
- [ ] 技术专家意见
- [ ] 其他支持材料

#### 申诉证据标准

| 证据类型 | 接受标准 | 说明 |
|----------|----------|------|
| 代码审查意见 | 具体漏洞引用 | 需包含代码行号 |
| 安全扫描报告 | Slither/类似工具 | 需包含扫描日期 |
| 第三方评估 | 签名评估机构 | 需包含评估日期 |
| 通信记录 | 可验证的截图 | 需包含时间戳 |
| 技术分析 | 详细技术说明 | 需包含方法论 |

#### 申诉委员会

| 角色 | 人数 | 要求 | 职责 |
|------|------|------|------|
| 长老 | 3人 | 声望≥5000 | 审核申诉材料 |
| 主席 | 1人 | 从长老中选举 | 主持会议、投票 |

**审核流程:**
1. 主席召集长老会议
2. 申诉人可申请陈述（可选）
3. 长老讨论并投票
4. 简单多数决（2/3+）
5. 发布裁决并执行

#### 裁决结果

| 裁决 | 条件 | 执行结果 |
|------|------|----------|
| 维持惩罚 | 2/3+长老同意原判 | 原惩罚生效 |
| 撤销惩罚 | 2/3+长老同意撤销 | 完全恢复 |
| 部分撤销 | 简单多数同意减轻 | 减轻惩罚程度 |

#### 自动通知系统

系统在以下时机自动通知相关方：

| 事件 | 通知对象 | 通知方式 |
|------|----------|----------|
| 惩罚触发 | 被惩罚者 | 链上事件 + 邮件 |
| 申诉窗口开启 | 被惩罚者 | 邮件 |
| 申诉材料提交 | 委员会 | 站内消息 |
| 申诉裁决发布 | 申诉人 + 验证者 | 链上事件 + 邮件 |
```
  </action>
  <verify>
    <automated>grep -c "### 5.3 状态机" SKILLS_STANDARD.md && grep -c "### 5.4 错误码定义" SKILLS_STANDARD.md && grep -c "### 5.5 Gas成本估算" SKILLS_STANDARD.md && grep -c "### 6.3 申诉流程" SKILLS_STANDARD.md && grep -c "REG_001\|VAL_001\|CHN_001\|ATR_001\|STK_001" SKILLS_STANDARD.md</automated>
  </verify>
  <done>
    Section 5 and 6.3 complete with:
    - 5.3 状态机 expanded with core states, sub-states, complete flowchart, triggers
    - 5.4 错误码定义 new subsection with 6 modules and 35+ error codes
    - 5.5 Gas成本估算 new subsection with gas estimates and optimization tips
    - 6.3 申诉流程 expanded with timeline, committee, evidence standards
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Document -> Contracts | Standards document informs implementation |
| IPFS Content -> Chain | Off-chain content linked via metadataIPFS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Audit trail events | mitigate | Events append-only via contract design |
| T-03-02 | Repudiation | IPFS metadata | mitigate | Require cryptographic signature on upload |
| T-03-03 | Information Disclosure | Error message details | accept | Error codes are non-sensitive |
</threat_model>

<verification>
## Phase-level Verification

After execution, verify:

1. **Section 4 Completeness:**
   ```bash
   grep -E "### 4\.[1-5]" SKILLS_STANDARD.md | wc -l  # Should be 5 subsections
   grep "keccak256(abi.encodePacked" SKILLS_STANDARD.md  # Fingerprint algorithm
   grep -c "SKILL_CREATED\|SKILL_SUBMITTED\|VERIFIER_ASSIGNED" SKILLS_STANDARD.md  # 8 events minimum
   ```

2. **Section 5 Completeness:**
   ```bash
   grep -E "### 5\.[1-5]" SKILLS_STANDARD.md | wc -l  # Should be 5 subsections
   grep "DRAFT\|PENDING\|VERIFYING\|APPROVED\|ACTIVE\|REJECTED\|SLAUGHTERED" SKILLS_STANDARD.md  # All states
   grep "REG_001\|VAL_001\|CHN_001" SKILLS_STANDARD.md  # Error codes exist
   ```

3. **Table of Contents Updated:**
   ```bash
   grep "4.4 IPFS存储格式\|4.5 追溯验证测试\|5.4 错误码定义\|5.5 Gas成本估算" SKILLS_STANDARD.md
   ```
</verification>

<success_criteria>
## Success Criteria

1. **TRACE-01**: Fingerprint algorithm documented with complete formula, inputs, outputs
2. **TRACE-02**: Minimum 8 audit trail events defined in table format
3. **TRACE-03**: IPFS storage format specified with on-chain/off-chain separation
4. **TRACE-04**: 15 traceability verification test cases documented
5. **PROC-01**: Complete state machine with 7 core states and 6 sub-states
6. **PROC-02**: Error codes covering all failure modes (35+ codes across 6 modules)
7. **PROC-03**: Gas cost estimates for all key operations
8. **PROC-04**: Appeal process with 11-day timeline, committee structure
9. All sections cross-referenced properly
10. Document structure maintains consistency with Phase 1-2 patterns
</success_criteria>

<output>
After completion, create `.planning/phases/03-tracing-process/03-01-PLAN-SUMMARY.md`
</output>
