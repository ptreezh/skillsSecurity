---
phase: 14-skill-attribution-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/contracts/SkillRegistry.test.cjs
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02
  - SKIL-03
  - SKIL-04

must_haves:
  truths:
    - "LOW risk skill can be registered without reputation check"
    - "MEDIUM risk skill requires 500+ effective reputation"
    - "HIGH risk skill requires 2000+ effective reputation"
    - "CRITICAL risk skill requires 5000+ effective reputation"
    - "Fingerprint generation produces consistent hashes"
    - "Skill verification requires effective reputation threshold per risk level"
    - "Successful verification triggers StakingManager.setPositiveContribution"
  artifacts:
    - path: "test/contracts/SkillRegistry.test.cjs"
      provides: "SKIL-01 to SKIL-04 test coverage"
      lines: 200+
  key_links:
    - from: "SkillRegistry.test.cjs"
      to: "StakingManager.getUserReputation()"
      via: "slashLiker to create reputation"
    - from: "SkillRegistry.test.cjs"
      to: "SkillRegistry.verifySkill"
      via: "positive contribution flow"
---

<objective>
SkillRegistry contract full test coverage for SKIL-01 to SKIL-04.

Requirements:
- SKIL-01: Reputation tier gates (L1-L5 thresholds) enforce correctly based on thresholds
- SKIL-02: Fingerprint generation produces consistent hashes for verification
- SKIL-03: Skill verification request -> approval flow completes end-to-end
- SKIL-04: getUserReputation integration with StakingManager

Output: `test/contracts/SkillRegistry.test.cjs` with complete SKIL-01 to SKIL-04 coverage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@test/fixtures.cjs
@test/contracts/StakingManager.test.cjs
@contracts/SkillRegistry.sol
@contracts/StakingManager.sol

## Key Technical Details

### Reputation Thresholds (per D-23 to D-26)
| Risk Level | Register Threshold | Verify Threshold |
|------------|-------------------|------------------|
| LOW | 0 (no check) | 100 |
| MEDIUM | 500 | 500 |
| HIGH | 2000 | 1000 |
| CRITICAL | 5000 | 2000 |

### Token Setup Pattern (Critical - from PITFALL #1)
```javascript
// User needs tokens AND contract needs approval
await token.transfer(user1, ethers.parseEther("100"));
await token.connect(user1).approve(registry.target, ethers.parseEther("100"));
```

### Reputation Setup via slashLiker (from RESEARCH.md)
```javascript
await staking.slashLiker(user1.address, 500, "Test reputation"); // Directly adds to userReputation
const effective = await staking.getUserReputation(user1.address);
```

### Event Names
- SkillRegistered(address indexed owner, uint256 indexed skillId, string name)
- SkillVerified(address indexed auditor, uint256 indexed skillId)
- FingerprintGenerated(uint256 indexed skillId, bytes32 fingerprint)

### RiskLevel Enum
- RiskLevel.LOW = 0
- RiskLevel.MEDIUM = 1
- RiskLevel.HIGH = 2
- RiskLevel.CRITICAL = 3

### MIN_STAKE Constants
- MIN_STAKE_LOW = 10 ether
- MIN_STAKE_MEDIUM = 50 ether
- MIN_STAKE_HIGH = 100 ether
- MIN_STAKE_CRITICAL = 200 ether
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKIL-01 Reputation Tier Gates Tests</name>
  <files>test/contracts/SkillRegistry.test.cjs</files>
  <read_first>
    - test/contracts/SkillRegistry.test.cjs (will be created)
    - contracts/SkillRegistry.sol (reputation threshold logic lines 77-91)
  </read_first>
  <action>
Create `test/contracts/SkillRegistry.test.cjs` with `describe("registerSkill - Reputation Thresholds")` section:

1. **LOW risk (RiskLevel.LOW = 0):** Should register without reputation check
   - Setup: transfer 100 tokens to user1, approve registry
   - Action: registry.connect(user1).registerSkill("LOWSkill", "", "", "", 0, "v1")
   - Verify: emits SkillRegistered event

2. **MEDIUM risk (RiskLevel.MEDIUM = 1):** Reverts when effective reputation < 500
   - Setup: user1 has 0 reputation
   - Action: registry.connect(user1).registerSkill("MEDIUMSkill", "", "", "", 1, "v1")
   - Verify: reverts with "Insufficient effective reputation for MEDIUM skill"

3. **MEDIUM risk with sufficient reputation:** Should register
   - Setup: staking.slashLiker(user1.address, 500, "Test") gives 500 reputation, transfer 100 tokens, approve
   - Action: registry.connect(user1).registerSkill("MEDIUMSkill", "", "", "", 1, "v1")
   - Verify: emits SkillRegistered event

4. **HIGH risk requires 2000+ effective reputation**
   - Setup: staking.slashLiker(user1.address, 2000, "Test"), transfer 200 tokens, approve
   - Action: registry.connect(user1).registerSkill("HIGHSkill", "", "", "", 2, "v1")
   - Verify: emits SkillRegistered event

5. **HIGH risk reverts when < 2000 reputation**
   - Setup: user1 has only 1000 reputation (via slashLiker with 1000)
   - Action: registry.connect(user1).registerSkill("HIGHSkill", "", "", "", 2, "v1")
   - Verify: reverts with "Insufficient effective reputation for HIGH skill"

6. **CRITICAL risk requires 5000+ effective reputation**
   - Setup: staking.slashLiker(user1.address, 5000, "Test"), transfer 300 tokens, approve
   - Action: registry.connect(user1).registerSkill("CRITICALSkill", "", "", "", 3, "v1")
   - Verify: emits SkillRegistered event

7. **CRITICAL risk reverts when < 5000 reputation**
   - Setup: user1 has 4000 reputation
   - Action: registry.connect(user1).registerSkill("CRITICALSkill", "", "", "", 3, "v1")
   - Verify: reverts with "Insufficient effective reputation for CRITICAL skill"

Token amounts: LOW=10, MEDIUM=50, HIGH=100, CRITICAL=200 (use 300 to be safe for all).
  </action>
  <verify>
    <automated>npx hardhat test test/contracts/SkillRegistry.test.cjs --grep "registerSkill - Reputation"</automated>
  </verify>
  <done>
    - LOW skill registers without reputation check
    - MEDIUM skill requires 500+ effective reputation
    - HIGH skill requires 2000+ effective reputation
    - CRITICAL skill requires 5000+ effective reputation
    - All reputation gates revert with correct error messages
  </done>
</task>

<task type="auto">
  <name>Task 2: SKIL-02 Fingerprint Generation Tests</name>
  <files>test/contracts/SkillRegistry.test.cjs</files>
  <read_first>
    - test/contracts/SkillRegistry.test.cjs (appending to existing file)
    - contracts/SkillRegistry.sol (computeFingerprint function lines 56-62)
  </read_first>
  <action>
Append `describe("computeFingerprint")` section to the test file:

1. **Consistent fingerprint:** Same inputs produce same hash
   - Action: Call registry.computeFingerprint("QmTest", user1.address, 1234567890) twice
   - Verify: Both results equal

2. **Different ipfsHash produces different fingerprint**
   - Action: computeFingerprint("QmA", user1.address, 1000) vs ("QmB", user1.address, 1000)
   - Verify: fingerprints are different

3. **Different creator produces different fingerprint**
   - Action: computeFingerprint("QmTest", user1.address, 1000) vs ("QmTest", user2.address, 1000)
   - Verify: fingerprints are different

4. **Different timestamp produces different fingerprint**
   - Action: computeFingerprint("QmTest", user1.address, 1000) vs ("QmTest", user1.address, 2000)
   - Verify: fingerprints are different

5. **Stored fingerprint matches computed fingerprint on registration**
   - Setup: transfer 100 tokens to user1, approve registry
   - Action: registry.connect(user1).registerSkill(...), capture transaction receipt
   - Verify:
     - Call registry.getFingerprint(0) to get stored fingerprint
     - Compute expected fingerprint from ipfsHash, user1.address, block.timestamp from receipt
     - Both equal (use 0-based skillId since first registration)
  </action>
  <verify>
    <automated>npx hardhat test test/contracts/SkillRegistry.test.cjs --grep "computeFingerprint"</automated>
  </verify>
  <done>
    - computeFingerprint produces consistent hashes for same inputs
    - Different inputs produce different hashes
    - Stored fingerprint matches computed fingerprint from registration
  </done>
</task>

<task type="auto">
  <name>Task 3: SKIL-03 and SKIL-04 Skill Verification Flow Tests</name>
  <files>test/contracts/SkillRegistry.test.cjs</files>
  <read_first>
    - test/contracts/SkillRegistry.test.cjs (appending to existing file)
    - contracts/SkillRegistry.sol (verifySkill function lines 118-147)
    - contracts/StakingManager.sol (setPositiveContribution lines 175-181)
  </read_first>
  <action>
Append `describe("verifySkill")` section to the test file:

1. **verifySkill LOW risk (requires 100 effective reputation)**
   - Setup: Register LOW skill (user1), give user2 100 reputation via slashLiker
   - Action: registry.connect(user2).verifySkill(0, true)
   - Verify: emits SkillVerified with (user2.address, 0)

2. **verifySkill MEDIUM risk (requires 500 effective reputation)**
   - Setup: Register MEDIUM skill (needs 500 reputation for registration too), give verifier 500 reputation
   - Action: registry.connect(verifier).verifySkill(0, true)
   - Verify: emits SkillVerified

3. **verifySkill HIGH risk (requires 1000 effective reputation)**
   - Setup: Register HIGH skill (needs 2000 reputation for registration), give verifier 1000 reputation
   - Action: registry.connect(verifier).verifySkill(0, true)
   - Verify: emits SkillVerified

4. **verifySkill CRITICAL risk (requires 2000 effective reputation)**
   - Setup: Register CRITICAL skill (needs 5000 reputation), give verifier 2000 reputation
   - Action: registry.connect(verifier).verifySkill(0, true)
   - Verify: emits SkillVerified

5. **verifySkill reverts when verifier has insufficient reputation**
   - Setup: user1 registers LOW skill, user2 has 50 reputation (< 100)
   - Action: registry.connect(user2).verifySkill(0, true)
   - Verify: reverts with "Insufficient effective reputation"

6. **verifySkill with _pass=true triggers setPositiveContribution (SKIL-04)**
   - Setup: user1 registers LOW skill, user2 has 100 reputation
   - Action: registry.connect(user2).verifySkill(0, true)
   - Verify: StakingManager emits PositiveContributionSet event with (user2.address)
   - Also verify: staking.hasPositiveContribution(user2.address) == true

7. **verifySkill with _pass=false does NOT trigger setPositiveContribution**
   - Setup: user1 registers LOW skill, user2 has 100 reputation
   - Action: registry.connect(user2).verifySkill(0, false)
   - Verify: No PositiveContributionSet event emitted

8. **verifySkill reverts for invalid skillId**
   - Setup: No skills registered
   - Action: registry.connect(user2).verifySkill(999, true)
   - Verify: reverts with "Invalid skill"

Token amounts for registration: LOW=10, MEDIUM=50, HIGH=100, CRITICAL=200 (add extra for safety).
Reputation amounts: Use slashLiker with exact threshold values to test boundary conditions.
  </action>
  <verify>
    <automated>npx hardhat test test/contracts/SkillRegistry.test.cjs --grep "verifySkill"</automated>
  </verify>
  <done>
    - verifySkill enforces reputation thresholds per risk level (100/500/1000/2000)
    - Successful verification triggers StakingManager.setPositiveContribution
    - Failed verification (_pass=false) does not trigger positive contribution
    - Invalid skillId reverts correctly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> contracts | Tests call contract functions, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | SkillRegistry.registerSkill | mitigate | Test reputation gates enforce L1-L5 thresholds correctly |
| T-14-02 | Information Disclosure | computeFingerprint | accept | Fingerprint is public by design, hash collision not practical |
</threat_model>

<verification>
- Run: `npx hardhat test test/contracts/SkillRegistry.test.cjs`
- All tests should pass
- Coverage should show SKIL-01 to SKIL-04 at 100%
</verification>

<success_criteria>
- [ ] SkillRegistry.test.cjs created with SKIL-01 to SKIL-04 coverage
- [ ] All 3 describe sections (registerSkill reputation, computeFingerprint, verifySkill) implemented
- [ ] Token setup pattern (transfer + approve) used correctly
- [ ] Reputation setup via slashLiker used correctly
- [ ] All tests pass: `npx hardhat test test/contracts/SkillRegistry.test.cjs`
</success_criteria>

<output>
After completion, create `.planning/phases/14-skill-attribution-tests/14-01-SUMMARY.md`
</output>