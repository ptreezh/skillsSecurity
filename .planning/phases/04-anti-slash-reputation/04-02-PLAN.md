---
phase: "04-anti-slash-reputation"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "SKILLS_STANDARD.md"
autonomous: true
gap_closure: true
requirements:
  - INT-01
  - INT-02
  - INT-04

must_haves:
  truths:
    - "Contract references match actual StakingManager.sol implementation"
    - "ABI section lists only functions that exist in contracts"
    - "Section 6.2 describes actual slashLiker/slash functions"
    - "Test coverage requirements table added per section"
  artifacts:
    - path: "SKILLS_STANDARD.md"
      issues_fixed:
        - "Removed non-existent ReputationLock struct from section 6.2"
        - "Replaced slashUser with slash and slashLiker in section 6.2"
        - "Replaced slashUser with slash and slashLiker in section 9.2 ABI"
        - "Added test coverage matrix per section"
---

<objective>
Fix integration gaps identified by verifier: contract reference mismatches and missing test coverage matrix.

Purpose: Align documentation with actual contract implementations and add test coverage requirements.
Output: Fixed Sections 6.2, 9.2, new test coverage table, and TOC/section anchor corrections.
</objective>

<context>
@SKILLS_STANDARD.md
@contracts/StakingManager.sol
@.planning/phases/04-anti-slash-reputation/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Section 6.2 to match actual StakingManager.sol implementation</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>
    - SKILLS_STANDARD.md (lines 1140-1226 - current Section 6.2)
    - contracts/StakingManager.sol (lines 1-96 - actual contract functions)
  </read_first>
  <action>
## Task 1: Fix Section 6.2 Contract Code

### 1.1 Remove non-existent ReputationLock struct

**CURRENT (WRONG):**
```solidity
// 声望冻结结构
struct ReputationLock {
    uint256 lockedAmount;    // 冻结金额
    uint256 lockedUntil;     // 冻结截止时间
    uint256 monthlyRecovery;  // 每月可恢复额度
}
mapping(address => ReputationLock[]) public reputationLocks;
```

**REPLACE WITH:**
Note that the actual contract does NOT have a ReputationLock struct. Document the simple reputation model instead.

### 1.2 Replace slashUser with actual contract functions

**CURRENT (WRONG):**
```solidity
function slashUser(
    address _user,
    int256 _penalty,
    string memory _reason,
    string memory _evidenceHash
) external onlyOwner {
    // ...
}
```

**REPLACE WITH actual StakingManager.sol functions:**

```solidity
// 质押惩罚（仅admin）
function slash(
    address _user,
    uint256 _skillId,
    uint256 _amount
) external onlyOwner {
    StakeInfo storage info = stakes[_user][_skillId];
    require(info.amount >= _amount, "Insufficient stake");
    
    info.amount -= _amount;
    
    // 发送给举报者25%（宪法第三条）
    uint256 reporterReward = (_amount * 25) / 100;
    token.transfer(msg.sender, reporterReward);
    
    emit Slash(_user, _skillId, _amount);
}

// 声望惩罚（仅admin）
function slashLiker(
    address _liker,
    int256 _penalty,
    string memory _reason
) external onlyOwner {
    userReputation[_liker] += _penalty;  // 可为正或负
    hasLiked[_liker] = false;
    
    emit AntiSlash(_liker, _penalty, _reason);
}

// 查询声望
function getUserReputation(address _user)
    external view returns (int256) {
    return userReputation[_user];
}
```

### 1.3 Add interface compatibility notes

After the contract code, add a compatibility table:

```markdown
#### 接口兼容性说明 (INT-02)

| 文档描述 | 实际合约 | 差异说明 |
|----------|----------|----------|
| slashUser() | 不存在 | 分为 slash() 和 slashLiker() 两个函数 |
| ReputationLock struct | 不存在 | 当前版本使用简单的 userReputation mapping |
```

### 1.4 Remove fictional getRecoverableReputation and claimRecoverableReputation

The actual contract does NOT have these functions. Remove them from the code example.
  </action>
  <verify>
    <automated>grep -c "slashLiker" SKILLS_STANDARD.md && grep -c "function slash(" SKILLS_STANDARD.md && ! grep -q "ReputationLock" SKILLS_STANDARD.md</automated>
  </verify>
  <done>
Section 6.2 updated with:
- Removed non-existent ReputationLock struct
- Replaced slashUser with actual slash() and slashLiker() functions
- Added getUserReputation() function
- Added interface compatibility notes explaining differences
- Removed fictional recovery functions
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Section 9.2 ABI reference to match actual contract</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>
    - SKILLS_STANDARD.md (lines 1704-1776 - current Section 9.2 ABI)
    - contracts/StakingManager.sol (lines 1-96 - for ABI generation)
  </read_first>
  <action>
## Task 2: Fix Section 9.2 ABI Reference

### 2.1 Replace StakingManager.sol ABI

**CURRENT (WRONG - line 1736-1753):**
```json
[
  {
    "inputs": [{"internalType": "address", "name": "_user", "type": "address"}],
    "name": "getUserReputation",
    ...
  },
  {
    "inputs": [{"internalType": "address", "name": "_user", ...}, {"internalType": "int256", "name": "_penalty", ...}, ...],
    "name": "slashUser",  // <-- DOES NOT EXIST
    ...
  }
]
```

**REPLACE WITH actual StakingManager.sol ABI:**

```json
#### StakingManager.sol 主要接口

```json
[
  {
    "inputs": [],
    "name": "stake",
    "outputs": [],
    "stateMutability": "nonpayable",
    "type": "function"
  },
  {
    "inputs": [{"internalType": "uint256", "name": "_skillId", "type": "uint256"}],
    "name": "unstake",
    "outputs": [],
    "stateMutability": "nonpayable",
    "type": "function"
  },
  {
    "inputs": [{"internalType": "address", "name": "_user", "type": "address"}, {"internalType": "uint256", "name": "_skillId", "type": "uint256"}, {"internalType": "uint256", "name": "_amount", "type": "uint256"}],
    "name": "slash",
    "outputs": [],
    "stateMutability": "nonpayable",
    "type": "function"
  },
  {
    "inputs": [{"internalType": "address", "name": "_liker", "type": "address"}, {"internalType": "int256", "name": "_penalty", "type": "int256"}, {"internalType": "string", "name": "_reason", "type": "string"}],
    "name": "slashLiker",
    "outputs": [],
    "stateMutability": "nonpayable",
    "type": "function"
  },
  {
    "inputs": [{"internalType": "uint256", "name": "_skillId", "type": "uint256"}],
    "name": "likeSkill",
    "outputs": [],
    "stateMutability": "nonpayable",
    "type": "function"
  },
  {
    "inputs": [{"internalType": "address", "name": "_user", "type": "address"}],
    "name": "getUserReputation",
    "outputs": [{"internalType": "int256", "name": "", "type": "int256"}],
    "stateMutability": "view",
    "type": "function"
  },
  {
    "inputs": [],
    "name": "token",
    "outputs": [{"internalType": "contract ASKToken", "name": "", "type": "address"}],
    "stateMutability": "view",
    "type": "function"
  }
]
```

#### 事件签名

Update event signatures table:

| 事件 | 签名 |
|------|------|
| Staked | `Staked(address,uint256,uint256)` |
| Unstaked | `Unstaked(address,uint256,uint256)` |
| Slash | `Slash(address,uint256,uint256)` |
| AntiSlash | `AntiSlash(address,int256,string)` |

Note: The actual contract does NOT have a `ReputationChanged` event.
  </action>
  <verify>
    <automated>grep -A2 '"name": "slash"' SKILLS_STANDARD.md && grep -A2 '"name": "slashLiker"' SKILLS_STANDARD.md && ! grep -q '"name": "slashUser"' SKILLS_STANDARD.md</automated>
  </verify>
  <done>
Section 9.2 ABI updated with:
- Replaced slashUser with actual slash() function
- Added slashLiker() function
- Added stake(), unstake(), likeSkill() functions
- Updated event signatures to match actual contract
- Removed mention of non-existent ReputationChanged event
  </done>
</task>

<task type="auto">
  <name>Task 3: Add test coverage requirements table (INT-04)</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>
    - SKILLS_STANDARD.md (lines 1-50 - TOC location)
    - SKILLS_STANDARD.md (lines 708-753 - existing traceability tests)
  </read_first>
  <action>
## Task 3: Add Test Coverage Requirements Table

### 3.1 Create new section 9.2 (shift existing 9.2 to 9.3, etc.)

Add test coverage requirements as a new section. Place it after Section 9.4 反馈渠道 (line 1805):

```markdown
### 9.5 测试覆盖要求 (INT-04)

#### 测试覆盖矩阵

每个文档章节必须满足以下测试覆盖要求：

| 章节 | 单元测试 | 集成测试 | 模糊测试 | 覆盖率目标 |
|------|----------|----------|----------|------------|
| 2. 技能分类标准 | 风险等级枚举、分类决策树 | 与SkillRegistry集成 | 输入边界测试 | 90% |
| 3. 安全验证标准 | 验证流程、冲突解决 | 验证者分配、投票 | 恶意输入测试 | 85% |
| 4. 责任追溯标准 | 指纹计算、审计记录 | IPFS集成、链上存储 | - | 80% |
| 5. 上链流程标准 | 状态机转换、错误码 | 多合约交互 | 交易重放测试 | 90% |
| 6. 反噬机制标准 | slash函数、slashLiker函数 | 惩罚触发、申诉流程 | 批量惩罚测试 | 85% |
| 7. 积分/声望系统 | 声望计算、等级判定 | 与Attribution集成 | - | 80% |
| 8. 协议接口标准 | 接口函数签名 | 跨合约调用 | - | 95% |

#### 测试类型说明

**单元测试 (Unit Tests)**
- 每个函数独立测试
- 边界条件和异常路径覆盖
- Mock外部依赖

**集成测试 (Integration Tests)**
- 跨合约调用验证
- 状态一致性检查
- 事件触发验证

**模糊测试 (Fuzz Testing)**
- 随机输入生成
- 边界值测试
- 异常输入处理

#### 验证测试用例 (INT-04)

已在第 4.5 节定义的追溯验证测试（TV-01 到 TV-15）需按以下频率执行：

| 测试类型 | TV-01 到 TV-04 | TV-05 到 TV-08 | TV-09 到 TV-12 | TV-13 到 TV-15 |
|----------|----------------|----------------|----------------|----------------|
| 执行频率 | 每次技能注册 | 每次状态变更 | IPFS上传时 | 定期批量 |
| 预期通过率 | 100% | 100% | 100% | 100% |

#### 持续集成要求

- 所有测试必须在 CI 中自动执行
- 覆盖率低于目标时 PR 阻塞
- 每次合并前必须通过完整测试套件
```

### 3.2 Update section numbers after 9.5

Shift existing sections:
- 9.5 反馈渠道 -> 9.6 反馈渠道
- Update TOC accordingly
  </action>
  <verify>
    <automated>grep -c "测试覆盖要求" SKILLS_STANDARD.md && grep -c "单元测试" SKILLS_STANDARD.md && grep -c "INT-04" SKILLS_STANDARD.md</automated>
  </verify>
  <done>
Test coverage requirements section added:
- New section 9.5 with coverage matrix per chapter
- Test type definitions (unit, integration, fuzz)
- Verification test frequency table (TV-01 to TV-15)
- CI requirements specified
- Section numbers shifted accordingly
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix TOC and section anchor issues</name>
  <files>SKILLS_STANDARD.md</files>
  <read_first>
    - SKILLS_STANDARD.md (lines 10-50 - TOC)
    - SKILLS_STANDARD.md (lines 1323-1371 - sections 6.4 and 6.5)
  </read_first>
  <action>
## Task 4: Fix TOC and Section Anchor Issues

### 4.1 Update Table of Contents (lines 10-50)

**CURRENT (missing 6.4):**
```
- 6. 反噬机制标准
  - 6.1 反噬触发条件
  - 6.2 反噬执行合约
  - 6.3 申诉流程
  - 6.4 技能重建与版本更新   <- WRONG: this is actually 6.5
```

**REPLACE WITH:**
```
- 6. 反噬机制标准
  - 6.1 反噬触发条件
  - 6.2 反噬执行合约
  - 6.3 申诉流程
  - 6.4 惩罚恢复流程
  - 6.5 技能重建与版本更新
```

### 4.2 Fix section 6.5 anchor (line 1370)

**CURRENT (wrong anchor):**
```markdown
### 6.5 技能重建与版本更新 {#6-4-技能重建与版本更新}
```

**REPLACE WITH:**
```markdown
### 6.5 技能重建与版本更新 {#6-5-技能重建与版本更新}
```

### 4.3 Verify section anchors match TOC

All section anchors should match their numbers:
- `{#6-1-...}` for section 6.1
- `{#6-2-...}` for section 6.2
- `{#6-3-...}` for section 6.3
- `{#6-4-...}` for section 6.4 (惩罚恢复流程)
- `{#6-5-...}` for section 6.5 (技能重建与版本更新)
  </action>
  <verify>
    <automated>grep -c "6.4 惩罚恢复流程" SKILLS_STANDARD.md && grep -c "{#6-5-技能重建与版本更新}" SKILLS_STANDARD.md && ! grep -q "{#6-4-技能重建与版本更新}" SKILLS_STANDARD.md</automated>
  </verify>
  <done>
TOC and anchor issues fixed:
- TOC now includes 6.4 惩罚恢复流程
- TOC shows 6.5 技能重建与版本更新 (correct)
- Section 6.5 anchor fixed from {#6-4-...} to {#6-5-...}
- All section numbers now match TOC
  </done>
</task>

</tasks>

<verification>
## Gap Closure Verification

After execution, verify all gaps are closed:

1. **INT-01 (Contract References):**
   ```bash
   grep -c "slashLiker" SKILLS_STANDARD.md  # Should find slashLiker function
   grep -c "function slash(" SKILLS_STANDARD.md  # Should find slash function
   grep -c "ReputationLock" SKILLS_STANDARD.md  # Should find 0 (removed)
   ```

2. **INT-02 (Interface Compatibility):**
   ```bash
   grep -c "接口兼容性说明" SKILLS_STANDARD.md  # Should find compatibility notes
   grep -c "slashUser" SKILLS_STANDARD.md  # Should find 0 (doesn't exist)
   ```

3. **INT-04 (Test Coverage):**
   ```bash
   grep -c "测试覆盖要求" SKILLS_STANDARD.md  # Should find coverage table
   grep -c "单元测试" SKILLS_STANDARD.md  # Should find test types
   ```

4. **TOC/Anchor fixes:**
   ```bash
   grep "6.4 惩罚恢复流程" SKILLS_STANDARD.md  # Should exist in TOC
   grep "{#6-5-技能重建与版本更新}" SKILLS_STANDARD.md  # Should exist
   ```
</verification>

<success_criteria>
## Success Criteria

Gap closure complete when:
1. **INT-01**: All contract references in SKILLS_STANDARD.md match actual StakingManager.sol functions (slash, slashLiker, getUserReputation)
2. **INT-02**: Section 6.2 describes actual contract implementation, not fictional code
3. **INT-04**: Test coverage requirements table exists in document
4. All TOC entries and section anchors are synchronized
5. No mention of non-existent functions (slashUser, ReputationLock) except in compatibility notes explaining the difference
</success_criteria>

<output>
After completion, create `.planning/phases/04-anti-slash-reputation/04-02-SUMMARY.md`
</output>