---
phase: 11-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hardhat.config.cjs
  - package.json
autonomous: true
requirements:
  - TEST-01
  - TEST-03

must_haves:
  truths:
    - "npm install adds chai-matchers, network-helpers, verify, solidity-coverage packages"
    - "hardhat.config.cjs has polygonAmoy network with chainId 80002"
    - "hardhat.config.cjs no longer references polygonMumbai (chainId 80001)"
    - "Mocha test runner generates coverage reports when tests run"
  artifacts:
    - path: "node_modules/@nomicfoundation/hardhat-chai-matchers"
      provides: "Contract assertion matchers (emit, revertedWith)"
    - path: "node_modules/@nomicfoundation/hardhat-network-helpers"
      provides: "Time manipulation (evm_increaseTime, evm_mine), loadFixture"
    - path: "node_modules/@nomicfoundation/hardhat-verify"
      provides: "PolygonScan contract verification"
    - path: "node_modules/solidity-coverage"
      provides: "Coverage reporting"
  key_links:
    - from: "hardhat.config.cjs"
      to: "node_modules/@nomicfoundation/hardhat-verify"
      via: "require('@nomicfoundation/hardhat-verify')"
    - from: "hardhat.config.cjs"
      to: "node_modules/solidity-coverage"
      via: "hardhat.config.plugins[]"
---

<objective>
Install Hardhat test toolbox plugins and update network configuration.

Purpose: Establish foundation for all testing phases with proper assertion matchers, time manipulation, and coverage reporting.
Output: Updated package.json, updated hardhat.config.cjs, all plugins installed and functional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@hardhat.config.cjs
@package.json

# Contract interface reference (for fixture dependencies)
ASKToken.sol: constructor() takes no args, mints MAX_SUPPLY to deployer
StakingManager.sol: constructor(address _token) requires token address
SkillRegistry.sol: constructor(address _token, address _stakingManager) requires both
Attribution.sol: constructor() takes no args, requires setStakingManager() after deployment

# Decision references
D-06: Replace polygonMumbai (chainId 80001) with polygonAmoy (chainId 80002)
D-08: Install @nomicfoundation/hardhat-chai-matchers
D-09: Install @nomicfoundation/hardhat-network-helpers
D-10: Install @nomicfoundation/hardhat-verify
D-11: Install solidity-coverage
D-12: Use Mocha test runner (default Hardhat)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test toolbox plugins</name>
  <files>package.json</files>
  <read_first>
    - package.json
  </read_first>
  <action>
Install the following packages as dev dependencies:
```bash
npm install --save-dev @nomicfoundation/hardhat-chai-matchers @nomicfoundation/hardhat-network-helpers @nomicfoundation/hardhat-verify solidity-coverage
```

This satisfies:
- D-08: @nomicfoundation/hardhat-chai-matchers for emit, revertedWith assertions
- D-09: @nomicfoundation/hardhat-network-helpers for loadFixture, evm_increaseTime, evm_mine
- D-10: @nomicfoundation/hardhat-verify for PolygonScan verification (Phase 16)
- D-11: solidity-coverage for coverage reporting

After install, verify with: ls node_modules/@nomicfoundation/ | grep hardhat
  </action>
  <verify>
    <automated>ls node_modules/@nomicfoundation/ 2>/dev/null | grep -E "hardhat-chai-matchers|hardhat-network-helpers|hardhat-verify"</automated>
    <automated>ls node_modules/solidity-coverage 2>/dev/null | head -1</automated>
  </verify>
  <acceptance_criteria>
    - [ ] node_modules/@nomicfoundation/hardhat-chai-matchers exists
    - [ ] node_modules/@nomicfoundation/hardhat-network-helpers exists
    - [ ] node_modules/@nomicfoundation/hardhat-verify exists
    - [ ] node_modules/solidity-coverage exists
    - [ ] package.json devDependencies includes all four packages with version ^2.0.0 or higher
  </acceptance_criteria>
  <done>All 4 test toolbox packages installed and visible in node_modules/@nomicfoundation/</done>
</task>

<task type="auto">
  <name>Task 2: Update hardhat.config.cjs with plugins and network</name>
  <files>hardhat.config.cjs</files>
  <read_first>
    - hardhat.config.cjs
  </read_first>
  <action>
Update hardhat.config.cjs:

1. **Add require statements at top:**
```javascript
require("@nomicfoundation/hardhat-chai-matchers");
require("@nomicfoundation/hardhat-network-helpers");
require("@nomicfoundation/hardhat-verify");
require("solidity-coverage");
```

2. **Update networks section:**
- REMOVE polygonMumbai network entirely (chainId 80001 is deprecated)
- ADD polygonAmoy network with chainId 80002

```javascript
networks: {
  hardhat: {
    chainId: 31337
  },
  polygonAmoy: {
    url: process.env.POLYGON_AMOY_RPC || "https://rpc-amoy.polygon.technology",
    accounts: process.env.PRIVATE_KEY ? [process.env.PRIVATE_KEY] : [],
    chainId: 80002
  },
  polygon: {
    url: process.env.POLYGON_RPC || "",
    accounts: process.env.PRIVATE_KEY ? [process.env.PRIVATE_KEY] : [],
    chainId: 137
  }
}
```

This satisfies D-06: Replace polygonMumbai with polygonAmoy (chainId 80002).
  </action>
  <verify>
    <automated>grep -c "hardhat-chai-matchers\|hardhat-network-helpers\|hardhat-verify\|solidity-coverage" hardhat.config.cjs</automated>
    <automated>grep -c "polygonAmoy.*chainId.*80002" hardhat.config.cjs</automated>
    <automated>! grep -q "polygonMumbai" hardhat.config.cjs && echo "No Mumbai references"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] hardhat.config.cjs contains require("@nomicfoundation/hardhat-chai-matchers")
    - [ ] hardhat.config.cjs contains require("@nomicfoundation/hardhat-network-helpers")
    - [ ] hardhat.config.cjs contains require("@nomicfoundation/hardhat-verify")
    - [ ] hardhat.config.cjs contains require("solidity-coverage")
    - [ ] hardhat.config.cjs networks has polygonAmoy with chainId: 80002
    - [ ] hardhat.config.cjs networks has NO polygonMumbai reference
    - [ ] All 4 require statements present at file top
  </acceptance_criteria>
  <done>hardhat.config.cjs updated with all plugins and polygonAmoy network, no polygonMumbai references remain</done>
</task>

<task type="auto">
  <name>Task 3: Verify configuration works with hardhat test</name>
  <files>NONE (verification only)</files>
  <read_first>
    - hardhat.config.cjs
  </read_first>
  <action>
Run a quick verification that Hardhat can load the updated config:

```bash
npx hardhat --version
```

Then create a minimal smoke test to verify plugins load correctly:
```bash
npx hardhat test --grep "smoke" 2>&1 || true
```

Expected: No errors about missing plugins. If plugins fail to load, fix require statements.
  </action>
  <verify>
    <automated>npx hardhat --version 2>&1 | head -1</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `npx hardhat --version` outputs version number without errors
    - [ ] No plugin loading errors in output
  </acceptance_criteria>
  <done>Hardhat config loads successfully with all plugins registered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {npm registry → local} | Packages downloaded from npm, code review already done |
| {local → Polygon Amoy RPC} | External RPC endpoint, testnet only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | hardhat.config.cjs | mitigate | Use versioned packages (^2.0.0) to prevent unexpected updates |
| T-11-02 | Information Disclosure | package.json | accept | Dev dependencies only, not deployed to production |

</threat_model>

<verification>
- `npm ls @nomicfoundation/hardhat-chai-matchers` shows installed version
- `grep "polygonAmoy" hardhat.config.cjs` returns polygonAmoy config
- `grep "polygonMumbai" hardhat.config.cjs` returns no matches
- `npx hardhat --version` succeeds without errors
</verification>

<success_criteria>
- All 4 packages (chai-matchers, network-helpers, verify, solidity-coverage) installed
- hardhat.config.cjs updated with all plugin requires
- polygonMumbai removed, polygonAmoy added with chainId 80002
- Hardhat test runner can execute without config errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-infrastructure/11-01-SUMMARY.md`
</output>