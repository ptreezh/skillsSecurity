---
phase: "19"
plan: "04"
type: execute
wave: 4
depends_on:
  - "19-03"
files_modified:
  - scripts/growth/ReferralManager.js
  - scripts/growth/ContentRewards.js
  - scripts/growth/ViralMechanics.js
  - scripts/growth/CommunityBot.js
autonomous: true
must_haves:
  truths:
    - "ReferralManager drives user acquisition through rewards"
    - "ContentRewards incentivizes community contribution"
    - "ViralMechanics creates network effects"
    - "CommunityBot automates social engagement"
  artifacts:
    - path: "scripts/growth/ReferralManager.js"
      provides: "Referral system with multi-tier rewards"
      contains:
        - Unique referral codes
        - Reward tracking and distribution
        - Leaderboard and bonuses
    - path: "scripts/growth/ContentRewards.js"
      provides: "Content creator rewards and verification"
      contains:
        - Content type tiers (tutorial, bug report, showcase)
        - AI screening and community voting
        - Reward distribution schedule
    - path: "scripts/growth/ViralMechanics.js"
      provides: "Viral loops for organic growth"
      contains:
        - Skill sharing rewards
        - Achievement system
        - Social proof mechanics
    - path: "scripts/growth/CommunityBot.js"
      provides: "Automated community engagement"
      contains:
        - Discord/Twitter auto-responses
        - Welcome and onboarding flows
        - Milestone notifications
---

<objective>
Implement Autonomous Promotion System: Referral, Content Rewards, Viral Mechanics, Community Bot.
</objective>

<context>
@.planning/phases/19-autonomous-operations/19-RESEARCH.md
@contracts/contracts/StakingManager.sol
</context>

<tasks>

<task>
<name>Task 01: Create ReferralManager</name>
<files>scripts/growth/ReferralManager.js</files>
<action>
Create scripts/growth/ReferralManager.js:

```javascript
/**
 * ReferralManager
 * Referral system with multi-tier rewards
 *
 * Rewards:
 * - Referrer: 1000 ASK per successful referral
 * - Referee: 500 ASK + 1 month Pro trial
 * - Bonus: 10x for first 100 referrals (Pioneer status)
 */

const crypto = require('crypto')

class ReferralManager {
  constructor(config) {
    this.stakingManager = config.stakingManager
    this.tokens = config.tokens
    this.rewards = {
      referrer: 1000,      // ASK tokens
      referee: 500,
      trialDays: 30
    }
    this.bonus = {
      threshold: 100,      // First 100 referrals
      multiplier: 10      // 10x reward
    }
    this.referrals = new Map() // code -> { referrer, count }
  }

  generateCode(address) {
    // Unique referral code per user
    const hash = crypto.createHash('sha256')
    hash.update(address + Date.now())
    return hash.toString('hex').substring(0, 8)
  }

  async trackReferral(referralCode, refereeAddress) {
    const referrer = this.referrals.get(referralCode)?.referrer
    if (!referrer) throw new Error('Invalid referral code')

    // Check if referee is new
    const isNew = await this.isNewUser(refereeAddress)
    if (!isNew) throw new Error('User already exists')

    // Calculate reward
    const count = await this.getReferralCount(referrer)
    const multiplier = count < this.bonus.threshold ? this.bonus.multiplier : 1

    const referrerReward = this.rewards.referrer * multiplier
    const refereeReward = this.rewards.referee

    // Distribute rewards
    await this.tokens.transfer(referrer, referrerReward)
    await this.tokens.transfer(refereeAddress, refereeReward)
    await this.upgradeTrial(refereeAddress)

    // Track
    const ref = this.referrals.get(referralCode)
    ref.count++
    ref.successful++

    return { referrerReward, refereeReward, multiplier }
  }

  async getReferralCount(address) {
    // Count successful referrals
  }

  async getLeaderboard() {
    // Top 10 referrers this month
  }

  async upgradeTrial(address) {
    // Give 1 month Pro trial
  }
}

module.exports = { ReferralManager }
```

Commit: "feat(19): create ReferralManager for user acquisition"
</action>
<verify>
- [ ] ReferralManager class exported
- [ ] Code generation works
- [ ] Reward calculation correct
</verify>
</task>

<task>
<name>Task 02: Create ContentRewards</name>
<files>scripts/growth/ContentRewards.js</files>
<action>
Create scripts/growth/ContentRewards.js:

```javascript
/**
 * ContentRewards
 * Content creator rewards and verification
 *
 * Content Types & Rewards:
 * - Tutorial: 500-5000 ASK (views + quality)
 * - Bug report: 200-2000 ASK (severity)
 * - Skill showcase: 100-1000 ASK (engagement)
 * - Social posts: 50-500 ASK (reach)
 *
 * Distribution:
 * - Daily pool: 10,000 ASK
 * - Weekly leaderboard: 50,000 ASK
 * - Monthly grants: 200,000 ASK
 */

class ContentRewards {
  constructor(config) {
    this.treasury = config.treasury
    this.dailyPool = 10000
    this.weeklyPool = 50000
    this.monthlyPool = 200000
    this.pending = []
  }

  async submit(content) {
    // Types: tutorial, bug_report, showcase, social
    const reward = this.calculateReward(content)

    // AI screening first
    const screened = await this.aiScreen(content)
    if (!screened.passed) {
      return { status: 'rejected', reason: screened.reason }
    }

    // Add to pending for community voting
    this.pending.push({
      id: this.generateId(),
      content,
      reward,
      submittedAt: Date.now(),
      votes: 0
    })

    return { status: 'pending', id: this.pending[this.pending.length - 1].id, reward }
  }

  async vote(contentId, voter, support) {
    const content = this.pending.find(c => c.id === contentId)
    if (!content) throw new Error('Content not found')

    content.votes += support ? 1 : -1

    // Auto-approve if high votes
    if (content.votes >= 10) {
      await this.distribute(content)
    }
  }

  calculateReward(content) {
    const tiers = {
      tutorial: { base: 500, max: 5000, criteria: 'views' },
      bug_report: { base: 200, max: 2000, criteria: 'severity' },
      showcase: { base: 100, max: 1000, criteria: 'engagement' },
      social: { base: 50, max: 500, criteria: 'reach' }
    }

    const tier = tiers[content.type]
    return Math.min(tier.max, tier.base * this.getQualityMultiplier(content))
  }

  async aiScreen(content) {
    // Check for spam, quality, relevance
    return { passed: true } // Simplified
  }

  async distribute(content) {
    const index = this.pending.indexOf(content)
    this.pending.splice(index, 1)

    await this.treasury.transfer(content.author, content.reward)
    return { status: 'distributed', reward: content.reward }
  }

  async getLeaderboard(period = 'week') {
    // Weekly top content creators
  }
}

module.exports = { ContentRewards }
```

Commit: "feat(19): create ContentRewards for creators"
</action>
<verify>
- [ ] ContentRewards class exported
- [ ] AI screening implemented
- [ ] Reward distribution correct
</verify>
</task>

<task>
<name>Task 03: Create ViralMechanics</name>
<files>scripts/growth/ViralMechanics.js</files>
<action>
Create scripts/growth/ViralMechanics.js:

```javascript
/**
 * ViralMechanics
 * Viral loops for organic growth
 *
 * Mechanics:
 * - Skill sharing: Share skill → get 10 ASK
 * - Achievement system: Badges, ranks, showcases
 * - Social proof: "X users created skills today"
 * - Streak rewards: Daily activity bonuses
 */

class ViralMechanics {
  constructor(config) {
    this.stakingManager = config.stakingManager
    this.referralManager = config.referralManager
    this.shareReward = 10  // ASK per share
    this.streakBonus = 1.5 // 50% extra for 7-day streak
  }

  async onSkillShare(skillId, channel) {
    // Channels: twitter, discord, telegram, link
    const user = await this.getSkillOwner(skillId)
    const reward = this.shareReward

    await this.stakingManager.mint(user, reward)

    // Track for virality score
    await this.trackShare(skillId, channel)

    return { reward, message: 'Earned 10 ASK for sharing!' }
  }

  async checkAchievements(address) {
    const achievements = []
    const userStats = await this.getUserStats(address)

    // Achievement tiers
    if (userStats.skillsCreated >= 1) {
      achievements.push({ id: 'first_skill', name: 'Creator', icon: '🌱' })
    }
    if (userStats.skillsCreated >= 10) {
      achievements.push({ id: 'skill_master', name: 'Skill Master', icon: '🌿' })
    }
    if (userStats.verifiedSkills >= 5) {
      achievements.push({ id: 'verified_pro', name: 'Verified Pro', icon: '✅' })
    }
    if (userStats.referrals >= 10) {
      achievements.push({ id: 'referral_champion', name: 'Referral Champion', icon: '🎯' })
    }
    if (userStats.streak >= 7) {
      achievements.push({ id: 'streak_week', name: '7-Day Streak', icon: '🔥' })
    }

    return achievements
  }

  async updateSocialProof() {
    // Returns current metrics for display
    const stats = await this.getPlatformStats()
    return {
      totalUsers: stats.users,
      totalSkills: stats.skills,
      activeToday: stats.activeToday,
      message: `${stats.activeToday} users created skills today! 🎉`
    }
  }

  async calculateStreakReward(address) {
    const streak = await this.getStreak(address)
    if (streak < 7) return 0
    return this.streakBonus
  }

  async getUserStats(address) {
    // Aggregates all user metrics
  }

  async trackShare(skillId, channel) {
    // Analytics for virality
  }
}

module.exports = { ViralMechanics }
```

Commit: "feat(19): create ViralMechanics for growth loops"
</action>
<verify>
- [ ] ViralMechanics class exported
- [ ] Achievement system implemented
- [ ] Social proof generation works
</verify>
</task>

<task>
<name>Task 04: Create CommunityBot</name>
<files>scripts/growth/CommunityBot.js</files>
<action>
Create scripts/growth/CommunityBot.js:

```javascript
/**
 * CommunityBot
 * Automated community engagement
 *
 * Channels:
 * - Discord: Welcome, onboarding, support
 * - Twitter: Milestones, user wins, announcements
 * - Telegram: Alerts, reminders, chat
 */

const { Client, GatewayIntentBits } = require('discord.js')

class CommunityBot {
  constructor(config) {
    this.discord = config.discordToken ? new Discord.Client({
      intents: [GatewayIntentBits.Guilds, GatewayIntentBits.GuildMessages]
    }) : null
    this.twitter = config.twitterConfig
    this.telegram = config.telegramToken
    this.welcomeMessage = this.getWelcomeMessage()
  }

  async start() {
    if (this.discord) {
      this.discord.on('guildMemberAdd', this.onMemberJoin.bind(this))
      this.discord.on('messageCreate', this.onMessage.bind(this))
      await this.discord.login(this.discordToken)
    }
  }

  async onMemberJoin(member) {
    const channel = await member.guild.channels.fetch('welcome-channel')
    await channel.send({
      embeds: [{
        title: 'Welcome to AgentSkills! 🎉',
        description: this.welcomeMessage,
        color: 0x2563eb
      }]
    })
  }

  async onMessage(message) {
    if (message.author.bot) return

    // Auto-responses
    if (message.content.includes('!help')) {
      await message.reply(this.getHelpMessage())
    }
    if (message.content.includes('!status')) {
      await message.reply(await this.getSystemStatus())
    }
    if (message.content.includes('!leaderboard')) {
      await message.reply(await this.getLeaderboardSnippet())
    }
  }

  getWelcomeMessage() {
    return `
Welcome to AgentSkills! 🚀

Here's how to get started:
1. Connect your wallet → /connect
2. Browse skills → /skills
3. Create your first skill → /create
4. Earn rewards → /rewards

Need help? Type !help for commands.

${this.getSocialProof()}
`
  }

  async postMilestone(milestone) {
    const tweet = `🎉 ${milestone}

We've reached ${milestone.count} ${milestone.type}!

Thank you to our amazing community 💙

#AgentSkills #Web3 #AI`

    await this.twitter.tweet(tweet)
  }

  async notifyNewSkill(skill) {
    const message = `✨ New skill created: ${skill.name}
By: ${skill.creator.slice(0, 8)}...
Risk: ${skill.riskLevel}
Reputation: ${skill.reputation}`

    // Post to all channels
    if (this.discord) {
      const channel = await this.discord.channels.fetch('announcements')
      await channel.send(message)
    }
    await this.twitter.tweet(message)
  }
}

module.exports = { CommunityBot }
```

Commit: "feat(19): create CommunityBot for automated engagement"
</action>
<verify>
- [ ] CommunityBot class exported
- [ ] Discord integration ready
- [ ] Auto-response triggers work
</verify>
</task>

</tasks>

<verification>
1. All 4 growth modules export correctly
2. Integration with contracts ready
3. Channel configurations valid

<success_criteria>
- ReferralManager drives acquisition
- ContentRewards incentivizes creators
- ViralMechanics creates network effects
- CommunityBot automates engagement
- All modules integrate with each other
</success_criteria>

<output>
After completion, create `.planning/phases/19-autonomous-operations/19-04-SUMMARY.md`
</output>